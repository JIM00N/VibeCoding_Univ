# 진행 기록 — 계모임

작업 로그·결정·발견된 문제를 한곳에 모은다. **이 파일이 진행 상황의 정본이다.**
기획은 [PRD.md](./PRD.md), 사용법·런북은 [README.md](./README.md).

## 기록 규칙

| 언제 | 어디에 | 무엇을 |
|---|---|---|
| 기능 하나(슬라이스)를 끝냈을 때 | §작업 로그 | 날짜 · 무엇 · **검증 방법** · 커밋 |
| 방향을 정했을 때 | §결정 | `D-n`으로 — 무엇을 / 왜 / **무엇을 포기했는지** |
| 문제를 만났을 때 | §문제와 처리 | `I-n`으로 — 증상 / 원인 / 처리 |

- **추측을 기록하지 않는다.** 라이브에서 확인한 것만 ✅로 적는다. 로컬만 통과한 건 🟡다.
- 결정은 지운 선택지까지 적는다. "왜 안 골랐는지"가 나중에 같은 논의를 막아준다.
- 문제는 해결됐어도 지우지 않는다. 재발할 때 검색해서 찾는 게 이 문서의 값어치다.

---

## 현재 상태

**1차(FR-1~13) + 2차(FR-14~17) 전부 구현 완료, 라이브 실측까지 끝남.** 컷 라인에 걸린 기능 없음.
**FR-18(회원가입)·FR-19(비밀번호 변경·찾기)까지 라이브 실측 완료.**

| FR | 기능 | 상태 | 검증 |
|---|---|---|---|
| 1·2 | 임시계정 로그인 / 로그아웃 | ✅ | 세션 쿠키로 신원 렌더 확인 |
| 3 | 모임 목록 (카드 그리드) | ✅ | 12개 · 멤버수 렌더 |
| 4·5 | 카테고리 · 지역 필터 | ✅ | `운동/스포츠 + 판교` = 정확히 3개 |
| 6 | 모임 상세 (소개 · 멤버 목록) | ✅ | 반코트 멤버 25명 |
| 7 | 가입 | ✅ | 멤버 25 → 26 |
| 8 | 탈퇴 | ✅ | 멤버 26 → 25, 참석 기록도 정리 |
| 9 | 검색 (필터와 동시 적용) | ✅ | `보드게임` → 1건 |
| 10 | 모임 만들기 | ✅ | 개설 즉시 홈 최상단 |
| 11 | 정모 목록 | ✅ | 셔틀 대회 · 일시 · 장소 |
| 12 | 정모 만들기 (모임장만) | ✅ | 새 모임에 정모 추가 |
| 13 | 참석 신청 / 취소 | ✅ | 참석 8 → 9 → 8 |
| X1 | 비로그인 시 `/login` 유도 | 🟡 | 코드 확인만 — 라이브 클릭 미실시 |
| X2 | 로그인 없이 조회 가능 | ✅ | 비로그인 curl로 목록·상세 200 |
| X3 | 데모 안내 배너 | ✅ | 전 페이지 상단 |
| **14** | **모임 채팅방** (멤버 전용, **WebSocket** + 폴백 폴링) | ✅ | 구독 SUBSCRIBED → 전송 200 → 소켓 수신 1건, 비멤버 403 · 비로그인 401 |
| **15** | **마이페이지** (`/me`, `/me/edit`) | ✅ | 가입 모임 7개 · 모임장 뱃지 · 프로필 렌더 |
| **16** | **회원 탈퇴** | ✅ | 아이디 오입력 시 세션 유지, 정확히 입력해야 삭제 |
| **17** | **모임 관리** (내보내기 · 정모 추가/삭제) | ✅ | 멤버 25 → 24, 모임장 아닌 사람은 307 |
| **18** | **회원가입** (`/signup`) | ✅ | 라이브에서 실패 3종(`id`·`pw2`·`taken`) 되돌아옴, 가입 → 303 + 세션 쿠키 → `/me`에 닉네임 렌더 → 새 계정으로 재로그인 성공 |
| **15+** | **이메일 등록·수정** (`/me/edit`, I-11) | 🟡 | 로컬 실측 — 형식 오류·누락 `error=email`, 저장 후 `/forgot` 재설정 성공. 임시계정엔 칸이 안 뜨고 저장해도 무시(demo01 `email` null 유지). 라이브 미확인 |
| **19** | **비밀번호 변경**(`/me/edit`) · **찾기**(`/forgot`) | ✅ | 라이브에서 찾기 실패 4종(`demo`·`nomatch`×2·`pw2`) + 재설정 성공 → 옛 비번 실패·새 비번 성공, 변경 3종(`pw-current`·`pw-demo`·성공) 확인. **demo01 은 여전히 `1234`**, demo 계정 `/me/edit`은 폼 대신 안내 |

**DB 상태**: 시드 전체 재실행으로 원복 완료 — 유저 40 · 모임 12 · 반코트 25명 · 박하은(demo03) 미가입 · 셔틀대회 참석 8 · 채팅 메시지 9 · 운동/스포츠+판교 3.
⚠️ **스키마 변경 1건** — `users.email` 컬럼 추가(nullable, FR-19 대조용). 시드 계정 40개는 `null`이고, 그 계정들은 비밀번호 변경 자체를 막으므로 시나리오에 영향이 없다. `db/schema.sql`에도 `alter table ... if not exists`로 반영했다.
🚨 **실제 사용자가 붙기 시작했다** — 시드가 아닌 계정 2개(`kuntae`/1조 조교, `jimoon`/문지석)와 그들이 만든 모임 1개(**「바이브코딩 모임」**, 업종/직무·용인, id 13)가 라이브에 있다. **`db/seed.sql`을 재실행하면 맨 위 TRUNCATE 가 이것까지 지운다.** 발표 전 리셋 여부는 이 데이터를 지워도 되는지 확인하고 결정할 것. (모임 13은 업종/직무·용인이라 `운동/스포츠+판교 = 3` 판정에는 영향 없음)

⚠️ **2026-08-20 회원가입 실측으로 유저가 42개다** — 테스트 계정 2개(`zz_test01`/`가입테스트`, `zz_live01`/`라이브가입테스트`)가 라이브 DB에 남아 있다. 둘 다 모임 가입·메시지가 없어서 시나리오 판정 숫자(반코트 25 · 운동/스포츠+판교 3 · 참석 8)는 그대로다. 발표 직전 시드 재실행 때 함께 사라진다.
여기에 **FR-19 실측용 `zz_pw01`(닉네임 `비번테스트`, 이메일 `zz.pw01@example.com`)이 하나 더 붙어 테스트 계정은 총 3개**다. 셋 다 모임 가입·메시지가 없다.
실제 유저 2명을 합쳐 **users 45행 · groups 13행**이 현재 상태다.

---

## 작업 로그

| 날짜 | 무엇 | 검증 | 커밋 |
|---|---|---|---|
| 2026-08-19 | 계획 수립 — 4시간 타임박스, 데모 시나리오 6단계, FR 우선순위·컷 라인 확정 | — | `0b76fbf` |
| 2026-08-19 | Next.js 16.3.1 스캐폴드 (App Router · TS · Tailwind v4 · Turbopack) | `npm run build` 통과 | `0b76fbf` |
| 2026-08-19 | Supabase 스키마 5테이블 + 시드(계정 40 · 모임 12 · 멤버십 214 · 정모 3 · 참석 24) | 시드 자가검증 4항목 통과 | — (MCP 직접 적용) |
| 2026-08-19 | `lib/` 상수·서버 클라이언트·HMAC 세션 / `app/actions.ts` 서버 액션 7종 | 빌드 통과 | `45d2571` |
| 2026-08-19 | 화면 4개 — 홈(필터·검색) · 로그인 · 모임 상세 · 모임 만들기 | 로컬 `next start` 실측 | `45d2571` |
| 2026-08-19 | `/api/health` 진단 엔드포인트 | 라이브 `db: ok, groupCount: 12` | (health 커밋) |
| 2026-08-19 | 라이브 실측 — FR 11개를 서버 액션 직접 호출로 확인 후 데이터 원복 | 아래 §검증 방법 | — |
| 2026-08-19 | 탈퇴 시 참석 기록 잔존 수정 + README 런북 | 라이브 참석 9 → 8 확인 | `7b7911d` |
| 2026-08-19 | 진행 기록 문서 신설(`PROGRESS.md`) + `CLAUDE.md` 기록 규칙 명문화 | — | `1819a75` |
| 2026-08-19 | **2차 기능** — 채팅방(FR-14) · 마이페이지(FR-15,16) · 모임 관리(FR-17). `messages` 테이블 + `users.bio` 추가 | 라이브에서 전송·내보내기·정모 추가·탈퇴 가드 전부 실측 후 시드 재실행으로 원복 | `d55291f` |
| 2026-08-19 | CI(lint+build) 도입 + 문서 반영. `main` 직접 커밋 폐기 → PR 흐름 | PR #30 CI 통과 후 머지, 라이브 7라우트 재확인 | `38eb8c7` |
| 2026-08-20 | **회원가입(FR-18)** — `/signup` 화면 + `signup` 서버 액션. 로그인 화면에 진입 링크 | 로컬 dev 서버에 서버 액션 직접 POST: 아이디 형식·대문자·짧은 비번·비번 불일치·닉네임 없음·중복 아이디 6종이 각각 `error=id/pw/pw2/nickname/taken`으로 되돌아오고 입력값이 보존됨. 가입 성공 시 `Set-Cookie` + `next`로 303, 그 쿠키로 `/me`에 닉네임 렌더, 새 계정으로 로그인 성공·오답 비번 실패, 로그인 상태로 `/signup` 재방문 시 `/me`로 307 | `0520ab1` |
| 2026-08-20 | **비밀번호 변경·찾기(FR-19)** — `/forgot` 화면 + `resetPassword`·`changePassword` 액션, 가입 폼에 이메일 추가, `users.email` 컬럼 신설 | 로컬 dev 서버에 액션 직접 POST: 변경 6종·찾기 5종 실패 경로가 각각 제 코드로 되돌아오고, 재설정 후 옛 비번 실패·새 비번 로그인 성공. 임시계정은 화면·서버 양쪽에서 거부(`pw-demo`), demo01 은 `1234` 유지 | (feat 커밋) |
| 2026-08-20 | **I-11 처리** — `/me/edit`에 이메일 필드 추가, 비어 있으면 `/me`·`/me/edit`에서 등록 재촉. 임시계정은 칸도 안 띄우고 액션에서도 무시 | 로컬 실측: 형식 오류·누락 `error=email`, 대문자 입력도 소문자로 저장, **이메일이 null이던 계정에 등록 → `/forgot` 재설정 → 새 비번 로그인 성공**. demo01 은 저장해도 `email` null·닉네임 그대로 | (feat 커밋) |
| 2026-08-20 | **모바일 반응형 정리(I-12)** — 하단 탭 바 신설, 헤더 계정 영역 `sm` 이상 한정, 모바일 입력칸 16px(iOS 확대 방지), 채팅 높이·모임 상세 버튼·관리 멤버 줄 좁은 화면 대응 | 헤드리스 Chrome(CDP, `mobile:true`) 실측 — 390/320px 전 페이지 가로 넘침 0, 탭 3개 렌더·활성표시 동작, 채팅은 입력창까지 한 화면에 들어옴. 639→640px에서 탭 폭 639→0으로 교대. 1280px 데스크톱 무변화. `npx tsc --noEmit` · `npm run lint` · `npm run build` 통과 | (이 커밋) |
| 2026-08-20 | 비밀번호 변경·찾기 **라이브 실측** — PR #34·#35 머지 후 프로덕션에서 확인 | 찾기 실패 4종·재설정 성공·옛 비번 실패·새 비번 로그인, 변경 `pw-current`/`pw-demo`/성공, demo01 `1234` 유지, `/signup` 이메일 입력칸 렌더. `/api/health` `db: ok` | `c8e60f5` (머지) |
| 2026-08-20 | 회원가입 **라이브 실측** — PR #33 머지 후 프로덕션에서 재확인 | 실패 3종이 각 `error` 코드로 303, 가입 성공 시 `Set-Cookie` + `next`로 303, 그 쿠키로 `/me`에 닉네임 렌더, 새 계정 재로그인 성공. `/api/health` `db: ok, groupCount: 12` | `b81392f` (머지) |
| 2026-08-19 | 채팅을 **소켓으로 전환** — Supabase Realtime 브로드캐스트 + 폴백 폴링 | 코드 작성 전 노드 스크립트로 설계 검증(publishable 키 구독 → REST 브로드캐스트 202 → 수신 확인), 이후 로컬 서버로 end-to-end 4단계 실측 | (feat 커밋) |

### 검증 방법 (브라우저 확장 없이 라이브를 검증한 방법)

Chrome 확장이 연결되지 않아 HTTP로 대체했다. 재현 절차:

1. **세션 위조** — `SESSION_SECRET` 미설정 시 기본값 `gyemoim-demo-secret`을 쓴다.
   `gyemoim_session=<userId>.<base64url(HMAC-SHA256(userId))>` 쿠키를 만들면 그 사용자로 로그인된다.
2. **서버 액션 호출** — Next.js는 no-JS 폴백으로 폼에 `$ACTION_ID_<hash>` 히든 필드를 넣는다.
   페이지 HTML에서 버튼 라벨로 폼을 찾아 액션 ID를 뽑고, 같은 URL에 POST한다.
3. ⚠️ **`Content-Type`은 반드시 `multipart/form-data`** — 폼의 `encType`이 그렇게 렌더된다.
   `application/x-www-form-urlencoded`로 보내면 **200이 돌아오지만 액션은 실행되지 않는다**(I-6).
4. 액션 ID 해시는 빌드마다 바뀔 수 있으므로 매번 다시 뽑는다.

---

## 결정

| # | 결정 | 왜 | 포기한 것 |
|---|---|---|---|
| D-1 | 백엔드를 분리하지 않고 Next.js 단일 앱 | 4시간에 두 개를 배포·연결할 시간이 없다. Server Component가 DB를 직접 읽으면 API 층 자체가 사라진다 | 이전 프로젝트의 FastAPI + Railway 구성 |
| D-2 | DB 접근은 `@supabase/supabase-js`(PostgREST) | HTTP 방식이라 서버리스 커넥션 풀 문제가 원천 소멸. 이전 프로젝트가 겪은 5432/6543 포트 이슈를 안 밟는다 | raw SQL의 표현력, `psql` 접근 |
| D-3 | 인증은 자체 세션(users 테이블 + HMAC 쿠키) | Supabase Auth는 이메일 형식·확인메일을 강요해서 "쉬운 id/pw"와 안 맞는다 | 비밀번호 재설정·소셜 로그인 |
| D-4 | 비밀번호 평문 저장 | 공개된 데모 계정이라 보호할 자산이 0. 해싱은 순수 비용 | 실서비스 전환 시 1번 교체 대상 |
| D-5 | 카테고리·지역은 테이블이 아니라 TS 상수 | 조인이 사라지고 필터가 단순 문자열 비교가 된다 | 런타임에 카테고리를 추가하는 기능 |
| D-6 | 커버 이미지 = 카테고리별 그라데이션 + 이모지 | 업로드는 최소 40분. 외부 이미지는 도메인 설정·CSP·로딩 실패를 달고 온다 | 실제 모임 사진 |
| D-7 | 멤버 수는 컬럼이 아니라 `memberships` 집계 | 청중 수십 명이 동시에 가입할 때 카운터가 틀어지는 사고를 차단 | 대용량에서의 집계 비용 |
| D-8 | 임시계정 8개가 아니라 **40개** | 시나리오 6에서 청중이 각자 다른 신원으로 들어와야 그림이 산다 | — |
| ~~D-9~~ | ~~PR 없이 `main` 직접 커밋~~ | 4시간 타임박스 한정. **D-15로 대체됨** | — |
| D-10 | RLS deny-by-default, 정책 **0개** | 모든 접근이 서버(service_role) 경유. anon 정책을 추가하면 브라우저에서 DB가 직접 열린다 | 클라이언트 직접 조회 |
| ~~D-11~~ | ~~채팅 갱신은 3초 폴링, Realtime 미채택~~ | **D-16으로 대체됨** — RLS 를 깨지 않고 소켓을 쓰는 방법이 있었다 | — |
| D-12 | **비밀번호 변경 미제공** → **임시계정 한정으로 축소**(2026-08-20, D-21) | 계정을 여러 사람이 나눠 쓴다. 한 명이 바꾸면 같은 계정을 쓰던 다른 청중이 못 들어온다. 이 이유는 **`demo*` 계정에만** 성립하므로, 직접 가입한 계정에는 변경·재설정을 허용한다 | (임시계정에 한해) 계정 보안 |
| D-13 | 회원 탈퇴는 실제 삭제하되 **로그인 아이디 입력 확인** 필수 | 공유 계정이라 지나가던 사람의 오클릭 한 번이 계정을 지운다. 사라지는 것(소유 모임 포함)을 개수로 미리 보여준다 | 한 번에 탈퇴하는 편의 |
| D-14 | 정모 생성 폼을 상세 → **관리 화면**으로 이동 | 관리 동선을 한곳에 모으고 상세 화면을 가볍게 유지 | 모임장의 클릭 한 번 |
| D-16 | 채팅은 **Supabase Realtime 브로드캐스트(WebSocket)**, 단 **신호만** 전송 | Vercel 서버리스는 WebSocket 서버를 띄울 수 없어 자체 `ws` 서버는 두 번째 배포처가 필요하다. Realtime 을 쓰되 **페이로드에 본문을 담지 않고** "새 메시지 왔다"만 쏜다 — 내용은 인증된 `GET /api/…/messages` 로만 나간다. 덕분에 **RLS 정책 0개(D-10)를 그대로 유지**하고, publishable 키가 노출돼도 읽을 수 있는 게 없다 | 비멤버도 "언제 메시지가 왔는지"라는 메타데이터는 알 수 있다 (내용은 못 봄) |
| D-17 | 소켓이 붙어도 **폴링을 없애지 않고** 간격만 20초로 늘림 | 소켓 순단·놓친 이벤트를 흡수하는 안전망. 공개 환경변수가 없으면 자동으로 3초 폴링으로 강등돼 채팅이 아예 죽지 않는다 | 완전한 이벤트 기반의 단순함 |
| D-18 | **정확한 지시가 없어 스스로 판단해야 하면 묻는다** | 사용자 지시(2026-08-19). 특히 머지·배포·데이터 삭제·범위 변경·사용자가 직접 만든 것을 덮어쓰는 일은 **매번 명시적 승인**을 받는다. "다 됐다"는 보고를 승인으로 읽지 않는다 | 판단을 대신 내려 진행 속도를 올리는 것. 대신 관례·읽으면 알 수 있는 사실·이미 내려진 결정의 적용은 묻지 않는다 |
| D-19 | **회원가입을 열되(FR-18) 비밀번호는 평문 그대로**(D-4 유지), 대신 가입 화면에 경고를 띄운다 | 사용자 지시(2026-08-20)로 범위에 들어왔다. 해싱을 넣으면 시드의 `1234` 계정 40개와 검증 경로가 갈라져 로그인이 두 갈래가 된다 — 데모 자산이 0인 상태에서 치를 비용이 아니다. 대신 **진짜 사람이 진짜 비밀번호를 넣는 사고**는 막아야 하므로 폼에 "평소 쓰는 비밀번호는 넣지 말라"고 명시했다 | 실사용 가능한 계정 보안. 실서비스 전환 시 D-4와 함께 1번으로 교체 |
| D-20 | 가입 아이디는 **영문 소문자·숫자·밑줄 4~20자**로 제한 | `login`은 아이디를 대소문자 구분해 그대로 비교한다. 대문자를 허용하면 `Demo01`로 가입한 사람이 `demo01`로 로그인하려다 실패한다 — 로그인 쪽을 건드리지 않고 입구에서 막는 편이 싸다 | 아이디에 한글·대문자·기호를 쓰는 자유 |
| D-23 | **실제 사용자 데이터가 라이브에 있어도 발표 전 시드 리셋은 그대로 간다.** `db/seed.sql`은 수정하지 않는다 | 사용자 판단(2026-08-20). 데모 판정 숫자를 흔들지 않는 쪽이 우선이고, 시드에 예외를 넣기 시작하면 리셋이 「전부 지우고 알려진 상태로」라는 성질을 잃는다 | 리셋 시점에 `kuntae`·`jimoon` 계정과 「바이브코딩 모임」이 함께 사라진다 — 알고 지르는 선택이다 |
| D-21 | **비밀번호 변경·찾기를 열되(FR-19) 본인 확인은 「아이디 + 이메일 대조」** — `users.email` 컬럼 추가 | 사용자 지시(2026-08-20). 진짜 재설정은 메일 발송이 필요한데 발송 연동은 범위 밖이다. 대안이던 **닉네임 대조는 탈락** — 닉네임이 멤버 목록에 그대로 공개돼 있어 청중이 서로 계정을 뺏을 수 있다. 이메일은 어디에도 노출하지 않으므로 그 사고가 안 난다. **D-12는 임시계정 한정으로 살아 있다** | 재설정 토큰·만료·메일 발송. 그래서 이메일을 아는 사람은 비밀번호를 바꿀 수 있다 — `/forgot` 화면에 그대로 적어뒀다 |
| D-22 | 가입 아이디에서 **`demo` 접두사를 예약어로** 막는다 | `isSeedAccount()`가 접두사 하나로 임시계정을 가른다. `demo41` 같은 계정을 새로 만들 수 있으면 "가입은 됐는데 비밀번호를 못 바꾸는" 계정이 생겨 판정이 흔들린다 | `demo`로 시작하는 아이디를 쓸 자유 |
| D-15 | **PR + CI 도입** (D-9 폐기) | 타임박스를 넘겨 계속 기능을 얹는 중이고 앱이 공개 배포돼 있다. `main` push = 즉시 프로덕션이라 게이트가 필요해졌다. 실익은 **PR 프리뷰 URL에서 먼저 클릭 검증**하는 것 — CI(lint + build)는 타입·린트만 막아준다 | 변경당 몇 분의 오버헤드 |
| D-24 | 모바일 동선은 **하단 탭 바**로 연다 (헤더 안의 햄버거 메뉴 아님) | 이번 문제는 「메뉴가 불편하다」가 아니라 「기능이 안 보여서 실행이 안 된다」였다. 탭 바는 핵심 3개를 **항상 켜 둔 채로** 보여주므로 한 번 더 눌러야 열리는 햄버거보다 이 증상에 정확히 대응한다. 소모임 앱의 관례이기도 해서 발표 때 설명이 필요 없다 | 탭 3칸(홈·모임 만들기·내 정보)에 안 들어가는 나머지 — **로그아웃은 `/me` 안으로 내려갔다.** 화면 아래 56px도 상시 내준다 |

---

## 문제와 처리

| # | 증상 | 원인 | 처리 |
|---|---|---|---|
| I-1 | Vercel: `No Next.js version detected` | 레포가 비어 있어 `package.json`이 없었다. Root Directory 설정 문제가 아니었다 | 스캐폴드 후 첫 push로 해소 |
| I-2 | 라이브 전 페이지 500 (digest만 노출) | 환경변수를 추가하기 **전** 배포를 친 것. 환경변수는 기존 배포에 소급 적용되지 않는다 | 재배포 후 해소. 원인 특정을 위해 `/api/health` 추가 |
| I-3 | 빌드 타입 에러 TS2352 | `supabase-js`는 생성된 타입이 없으면 many-to-one 임베드도 **배열**로 추론한다. PostgREST의 실제 응답은 객체 | `Embedded<T>` 타입 + `single()` 정규화 헬퍼로 양쪽을 다 받는다 |
| I-4 | 탈퇴해도 참석자 명단에 남음 | `leaveGroup`이 멤버십만 지우고 `attendances`를 안 건드렸다 | 그 모임 정모의 참석 행을 함께 삭제. 라이브 확인 완료 |
| I-5 | Chrome 확장 미연결 — 브라우저 자동 검증 불가 | 확장이 연결돼 있지 않음 | HTTP로 세션 위조 + 서버 액션 직접 호출로 대체. **육안 확인은 여전히 미실시** |
| I-6 | 서버 액션 POST가 200을 주는데 아무 일도 안 일어남 | `application/x-www-form-urlencoded`로 보냈다. Next.js 폼은 `multipart/form-data`로 렌더된다 | `curl -F`로 전환. **오류 없이 조용히 무시되므로 눈치채기 어렵다** |
| I-7 | 채팅 메시지가 폴링할 때마다 순서가 바뀔 수 있음 | `created_at` 하나로만 정렬 — 같은 초에 도착한 메시지의 순서가 비결정적이다 | `created_at` + `id` tie-break. 청중이 동시에 떠들면 바로 드러날 문제였다 |
| I-8 | 회원 탈퇴가 소유 모임을 통째로 지운다 | `groups.owner_id ... on delete cascade` — 탈퇴하면 그 사람이 만든 모임의 멤버·정모·채팅까지 사라진다 | 막지 않고 **경고 + 아이디 입력 확인**으로 처리(D-13). 소유권 이전 기능은 범위 밖 |
| I-10 | `CLAUDE.md` 가 에디터 저장으로 이전 버전으로 되돌아가고 마크다운이 깨짐 | 편집 전 버전을 열어둔 상태에서 저장 + 에디터가 강조 경계를 재배치(`` `**텍스트`** ``) | 두 버전을 백업한 뒤 원격본을 정본으로 복구하고 사용자가 추가한 규칙만 합침. **에디터로 이 파일을 편집할 땐 최신 원격본을 먼저 pull 할 것** |
| I-11 | **가입 뒤에 이메일을 등록·수정할 방법이 없다** | FR-19에서 `users.email`을 가입 폼에만 붙였다. `/me/edit`에는 이메일 칸이 없어서 ① 이메일 없던 시절에 가입한 `kuntae`가 `/forgot`을 영영 못 쓰고 ② 오타를 고칠 수단도 없었다 | `/me/edit` 프로필 폼에 이메일 필드 추가(일반 계정 필수·임시계정 미표시), 비어 있으면 `/me`와 `/me/edit`에서 등록을 재촉한다. 이메일이 null이던 계정에 등록 → `/forgot` 재설정 → 새 비번 로그인까지 실측 확인 |
| I-12 | **모바일 화면에서 「모임 만들기」·마이페이지가 아예 사라진다** | `components/header.tsx`가 두 링크에 `hidden sm:*`를 걸어 640px 미만에서 감췄는데, 대신 여는 메뉴를 안 만들었다. 좁은 화면에는 헤더에 검색창까지밖에 안 들어가서 「모임 만들기(FR-10)」와 「마이페이지(FR-15)」를 **볼 수도 실행할 수도 없었다**. 로고 옆 `xs:inline`은 Tailwind v4에 `xs` 브레이크포인트가 없어 죽은 클래스였다 | 하단 탭 `components/mobile-nav.tsx`(홈 · 모임 만들기 · 내 정보/로그인, `sm:hidden fixed`) 신설. 헤더의 계정 영역은 `hidden sm:flex`로 묶고 로그아웃은 `/me`로 내렸다. 390·320px 실측에서 `documentElement.scrollWidth == innerWidth`(가로 넘침 없음), 640px에서 탭이 사라지고 헤더가 이어받는 것까지 확인 |
| I-9 | Vercel 서버리스에 자체 WebSocket 서버를 띄울 수 없다 | 함수가 요청-응답 후 종료돼 영속 연결을 못 잡는다. SSE 도 인스턴스 간 pub/sub 백플레인이 없어 결국 DB 폴링이 된다 | Supabase Realtime(관리형 WebSocket)을 쓰되 신호만 나르는 설계로 우회(D-16) |

---

## 남은 일

- [ ] **브라우저 육안 리허설** — 시나리오 1~6을 실제로 클릭. 특히 모바일에서 카테고리·지역 칩이 가로 스크롤되는지, 채팅 화면 높이가 폰에서 맞는지 (I-5로 미검증)
- [ ] 채팅 소켓이 청중 수십 명 동시 접속에서 버티는지 — Supabase Realtime 무료 플랜의 동시 연결 한도 확인 필요
- [ ] Vercel 에 `NEXT_PUBLIC_SUPABASE_URL` · `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` 추가 후 **재빌드** (NEXT_PUBLIC 은 빌드 시점에 번들에 박힌다 — 추가만 하고 재배포 안 하면 소켓이 안 붙고 3초 폴링으로 동작)
- [ ] FR-X1 — 비로그인 상태에서 가입 버튼을 눌렀을 때 `/login`으로 가는지 라이브 확인
- [x] ~~발표 전 시드 리셋 여부 재검토~~ → **그대로 리셋한다**(D-23). 실제 사용자 데이터가 지워지는 것을 알고 가는 선택
- [ ] 모바일 하단 탭(I-12 처리분)을 실제 폰에서 확인 — 특히 iOS Safari 하단 툴바와 겹치는지, 입력칸 포커스 시 확대가 없는지
- [ ] 이메일 등록 폼(I-11 처리분)을 라이브에서 확인 — 특히 `kuntae`(이메일 null)에게 재촉 배너가 뜨는지
- [ ] FR-18 — 회원가입 폼을 **브라우저로 육안 확인** (HTTP 실측은 끝, 모바일에서 폼 높이·에러 문구 위치 미확인)
- [ ] 라이브 DB의 테스트 계정 3개(`zz_test01`·`zz_live01`·`zz_pw01`) 정리 여부 결정 (시드 재실행으로도 사라짐)
- [ ] 발표 **직전** `db/seed.sql` 재실행으로 리셋 (발표 중에는 금지 — TRUNCATE가 전부 지운다)
- [ ] 청중에게 `demo01`~`demo40` 계정 배분 (좌석·조별로 나누면 시나리오 6에서 안 겹친다)
