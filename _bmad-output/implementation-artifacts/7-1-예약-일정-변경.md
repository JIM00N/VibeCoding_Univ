---
epic: 7
story: 1
key: 7-1-예약-일정-변경
branch: story/7-1-appointment-reschedule
baseline_commit: a185a59c9332c2f3d8c695ff97bf315b9518ebf6
---

# Story 7.1: 예약 일정 변경 (담당 의사 + 진료 시각)

Status: done

> 근거: `planning-artifacts/sprint-change-proposal-2026-07-29.md`(승인) · `epics.md` Epic 7 · FR-19
> 착수 게이트: **없음** — Epic 1~6 전 스토리 done.

## Story

As a 접수 직원,
I want 대기·확정 예약의 담당 의사와 진료 시각을 한 번에 바꾸기를,
so that 취소 후 재예약 없이 일정을 조정하고 그 환자의 예약 이력이 끊기지 않는다.

## Acceptance Criteria

**AC1 — 일정 변경 성립**
**Given** 대기 또는 확정 상태의 예약이 있을 때
**When** 직원이 `[변경]`에서 담당 의사·날짜·시각을 골라 저장한다
**Then** `PATCH /appointments/{id}/reschedule`로 `doctor_id`·`reserved_at`이 **한 SQL 문에서** 갱신되고 응답이 기존 `AppointmentOut` 정규 모델로 온다 (FR-19, AD-10)
**And** `status`·`hospital_department_id`는 어떤 경로에서도 바뀌지 않는다 (AD-5)

**AC2 — 부분 변경과 무변경**
**Given** 변경 요청에서
**When** 의사만 / 시각만 / 둘 다 바꿔 제출한다
**Then** 세 경우 모두 성립한다(미지정 필드는 현재 값 유지)
**And** 의사·시각이 둘 다 현재 값과 같으면 400 한국어로 거부한다
**And** 기존 `"이미 담당하고 있는 의사예요"` 400 규칙은 **제거**된다 — 의사를 그대로 두고 시각만 바꾸는 것이 정당한 요청이 됐다

**AC3 — 변경 불가 상태**
**Given** 완료 또는 취소 상태의 예약일 때
**When** 변경을 시도한다
**Then** 400 한국어로 거부한다 — 2.3 의 상태별 분기 구조를 계승하되 **주어는 "일정"으로 갱신**한다(`"완료된 예약은 일정을 바꿀 수 없어요."`). 이 경로는 시각도 바꾸므로 "담당 의사를 바꿀 수 없어요"는 거짓 안내가 된다(UX-DR10 — 오류는 정직하게). *(2026-07-29 코드리뷰: 원안의 "바이트 그대로" 는 과대 명세였다. 지석님 결정으로 문구 갱신 채택 — `epics.md` Epic 7 AC3 과 일치)*
**And** 검증과 UPDATE 사이 status 경합은 CAS 가드가 막고 409 한국어(기존 `CAS_CONFLICT_DETAIL`)로 안내한다

**AC4 — 의사 축 가용성 재검사**
**Given** 새 (의사, 슬롯)이 이미 점유돼 있을 때
**When** 저장한다
**Then** 409 한국어로 거부하고 슬롯 피커의 그 셀을 `taken`으로 갱신한다 (FR-15, AD-4, UX-DR3, UX-DR7)
**And** **자기 행을 제외**한다(`exclude_appointment_id`) — 자기가 점유한 슬롯으로의 "변경"이 자기 자신과 충돌하지 않는다

**AC5 — 환자 축 중복 차단**
**Given** 그 환자가 새 슬롯에 이미 **다른** 활성(대기·확정) 예약을 갖고 있을 때
**When** 저장한다
**Then** `db/migrations/006` 부분 유니크 인덱스가 UPDATE 를 거부하고, 기존 `_reject_unique_violation`(제약 이름 확인)이 409 환자 축 문구로 매핑한다 (FR-15b)
**And** 매핑 코드를 새로 만들지 않는다 — 생성 경로와 **같은 함수**를 쓴다

**AC6 — 과거 시각 가드**
**Given** 새 시각이 이미 지난 슬롯일 때
**When** 저장한다
**Then** 400 한국어로 거부한다(생성 경로 `create_appointment`와 **바이트 동일 문구**: `"이미 지난 시간이에요. 다른 시간을 골라 주세요."`)
**And** 이 가드는 `reserved_at`이 **실제로 바뀔 때만** 적용해, 과거 예약의 의사만 바꾸던 기존 동작을 깨지 않는다 (2.3 회귀 방지 — 기존 테스트가 이 계약을 고정)

**AC7 — 자기 예약을 taken 에서 제외**
**Given** `[변경]` 다이얼로그의 슬롯 격자가
**When** 렌더된다
**Then** **그 예약 자신이 점유한 슬롯은 `taken`으로 표시되지 않는다** — `GET /availability`가 `exclude_appointment_id`를 받아 **의사 축·환자 축 둘 다**에서 자기 행을 뺀다 (UX-DR3)
**And** 파라미터 미지정 시 응답이 기존과 바이트 동일해 기존 호출자(환자 예약·대리 예약)가 무수정으로 동작한다

**AC8 — 행 액션 통합**
**Given** 직원 예약 관리 목록에서
**When** 행 액션이 렌더된다
**Then** `[의사 변경]`이 `[변경]`으로 대체되고 버튼 수는 그대로다(대기 3개·확정 3개)
**And** 390×844에서 카드 오버플로 0, 콘솔 오류 0 (UX-DR11)
**And** `[변경]`은 비파괴적 `Dialog` 다 — 취소만 `AlertDialog` 를 유지한다 (UX-DR6 무변경)

**AC9 — `/doctor` 폐기 완결**
**Given** `PATCH /appointments/{id}/doctor` 폐기 후
**When** 레포를 검색한다
**Then** 라우터·서비스·db SQL·스키마·`lib/api.ts` 함수·해당 계약 테스트가 **함께** 제거되고 살아있는 코드·규칙 파일에 남은 참조가 0이다
**And** 완료 스토리 파일(2-3 등)의 언급은 시점 기록물이라 **그대로 둔다**

## Tasks / Subtasks

- [x] **Task 0 — 착수 준비** (AC: 전체)
  - [x] `story/7-1-appointment-reschedule` 브랜치 · sprint-status `in-progress`
  - [x] 마이그레이션 **0건** 확인 — 이 스토리는 DB 를 건드리지 않는다(006 은 이미 라이브)
  - [x] 아래 "건드리지 않는 것" 절 정독

- [x] **Task 1 — 백엔드 TDD red** (AC: 1~6)
  - [x] `tests/test_appointments.py` 의 2.3 블록 **13건 + db 해석 단위 2건**을 `/reschedule` 로 이관·재작성(아래 "테스트 이관 표")
  - [x] 신규: 시각만 변경 성립 · 둘 다 변경 성립 · 무변경 400 · 새 시각 과거 400 · 환자 축 409 · 자기 슬롯 유지 성립
  - [x] `tests/test_availability.py` 신규: `exclude_appointment_id` 가 두 축 모두에 전달됨 · 미지정 시 기존 호출 인자 바이트 동일

- [x] **Task 2 — schemas** (AC: 1, 2)
  - [x] `AppointmentDoctorUpdate` → `AppointmentRescheduleUpdate`: `doctor_id: int | None = None`, `reserved_at: datetime | None = None`, `extra="forbid"` 유지
  - [x] 둘 다 None 은 **서비스가 400 한국어**로 막는다(Pydantic 필수로 두면 422 리스트 detail 이라 `lib/api.ts` 가 일반 메시지로 바꾼다 — AD-10)

- [x] **Task 3 — db 계층** (AC: 1, 4, 5, 6)
  - [x] `_UPDATE_APPOINTMENT_DOCTOR` → `_UPDATE_APPOINTMENT_SCHEDULE`: `set doctor_id = %(doctor_id)s, reserved_at = %(reserved_at)s`
  - [x] `taken` CTE 의 슬롯 식을 `"(select reserved_at from target)"` → `"%(reserved_at)s"` (**새 시각** 기준 판정)
  - [x] `target` CTE 는 `cas_ok` 판정용으로 유지(더 이상 `reserved_at` 을 유도하지 않음)
  - [x] `update_appointment_doctor` → `update_appointment_schedule(appointment_id, doctor_id, reserved_at, allowed_sources)`
  - [x] `_interpret_doctor_update_row` → `_interpret_schedule_update_row` (CAS 우선 규칙 **그대로**)

- [x] **Task 4 — services** (AC: 1~6)
  - [x] `set_appointment_doctor` → `set_appointment_schedule`
  - [x] 유효값 합성: `effective_doctor_id = payload.doctor_id ?? current["doctor_id"]` · `effective_slot = to_slot(payload.reserved_at ?? current["reserved_at"])`
  - [x] 가드 순서(계약): ① 둘 다 None → 400 ② 404 ③ status 부적격 → 400 ④ **무변경 → 400** ⑤ 의사 소속 검증(`_require_doctor_in_department`) ⑥ **시각 변경 시에만** 과거 가드 → 400 ⑦ UPDATE → `SlotTakenError` 409 · `UniqueViolation` → `_reject_unique_violation` · 0행 → CAS 409
  - [x] `"이미 담당하고 있는 의사예요"` 분기 **삭제**, 무변경 400 으로 대체

- [x] **Task 5 — routers** (AC: 1, 9)
  - [x] `PATCH /appointments/{id}/doctor` **삭제** → `PATCH /appointments/{id}/reschedule` 신설
  - [x] `PATCH /appointments/{id}`(status)·`GET`·`POST` 는 무수정

- [x] **Task 6 — `/availability` 자기 행 제외** (AC: 7)
  - [x] router: `exclude_appointment_id: int | None = None` 선택 파라미터 추가
  - [x] service `get_taken_slots` 로 통과
  - [x] db `select_taken_slots`: 하드코딩 `"exclude_appointment_id": None` → 인자
  - [x] db `_SELECT_PATIENT_TAKEN_SLOTS`: **제외 조건 자체가 없다** — `and (%(exclude_appointment_id)s::bigint is null or a.id <> %(exclude_appointment_id)s)` 추가 (의사 축 조각과 **같은 관용구**)

- [x] **Task 7 — 프런트 API 클라이언트** (AC: 1, 7, 9)
  - [x] `updateAppointmentDoctor` → `rescheduleAppointment(id, { doctor_id?, reserved_at? })`
  - [x] `getAvailability(doctorId, startIso, endIso, patientId?, excludeAppointmentId?)` — 마지막 선택 인자 추가(기존 4-인자 호출부 2곳 무수정)

- [x] **Task 8 — `[변경]` 다이얼로그** (AC: 2, 7, 8)
  - [x] `app/staff/appointments/page.tsx`: `[의사 변경]` → `[변경]`, `openDoctorChange` → `openReschedule`
  - [x] 다이얼로그에 **날짜 Select + SlotPicker** 추가 — `lib/booking-slots.ts`(`seoulDayOptions`·`slotsForSeoulDay`·`formatSeoulDayLabel`)·`components/slot-picker.tsx` 재사용, 재구현 금지
  - [x] 의사 Select 에서 **현재 의사 제외 필터를 없앤다** — 의사 유지 + 시각만 변경이 가능해야 한다(AC2)
  - [x] 가용성 조회: `getAvailability(effectiveDoctorId, start, end, target.patient_id, target.id)` — 두 축 받아 `unavailableMs = taken ∪ patient_taken`
  - [x] 열 때마다 remount(`key`) — 시각 계산이 "여는 시점" 기준(Epic 6 회고 액션 #2)
  - [x] 409 처리: red 인라인 + 그 셀 taken 즉시 마킹 + 선택 해제 + 재조회 nonce (`proxy-booking-dialog` 패턴)

- [~] **Task 9 — 검증** (브라우저 실측 2항목 미수행 — 아래 참조)
  - [x] `pytest -q` 전체 green · `ruff check .` 0건
  - [x] `npm run lint` 0건 · `npm run build` 통과(**타입 오류는 build 에서만 나온다**)
  - [x] 로컬 uvicorn + 실 Supabase **curl 실증**(아래 "curl 실증 시나리오" 6종)
  - [~] 브라우저 실측 390×844 — **부분 수행**. 지석님 수동 확인으로 8항목 통과(행 액션·기본값·자기 슬롯 미차단·두 축 taken·시각만 변경·같은 값 재선택·무변경 비활성·콘솔 0/오버플로 0). **미수행 2건**: `page.clock` 시간 경과(Playwright 전용) · 409 인라인(브라우저 재현). 아래 체크리스트가 정본
  - [x] **검증 데이터 SQL 원복** (처방 → 기록 → 예약 순)

- [x] **Task 10 — 릴리스 게이트** (done 정의 3게이트)
  - [x] Codex 사전 리뷰 + `/bmad-code-review` → 트리아지 → Patch 반영 — 4층 42건 → 22건 트리아지, Decision 1·Patch 16 전건 반영, Defer 5 기록, Dismiss 1
  - [x] PR → 사용자 확인 → merge commit(squash 아님) — PR #29 머지(512c7b1). 머지 전 `headRefOid` == 로컬 HEAD 확인(478da88), 머지 후 누락 점검 0건(잔여 2 SHA 는 내용 기준 기반영 확인 — b10fdcc→d16acfb cherry-pick, 85cb654 내용 동일)
  - [x] ⚠️ **Railway 배포 완료를 확인한 뒤** 라이브 실측 — Railway 선확인(`/doctor`→404 · `/reschedule`→400 · `exclude_appointment_id`→200) 후 실측. 라이브 경로 6종 통과(빈 바디 400·시각만·의사만·무변경 400·의사 축 409 새 문구·자기 행 제외 두 축). Vercel 번들에 새 문구 5종 확인, 옛 `의사 변경`·`담당 의사 변경` **소멸**
  - [x] 라이브 확인 → 원복 → Status·sprint-status `done` 커밋 — 검증 예약 147·148 삭제. DB 가 세션 시작 상태와 일치(예약 4·활성 1·기록 3·처방 4)

### Review Findings (2026-07-29)

4층 리뷰(Codex 사전 · Blind Hunter · Edge Case Hunter · Acceptance Auditor) 원발견 42건 → 중복 제거 후 22건.
심각도는 직원(주 소비자) 기준으로 재평가했고, 서브에이전트가 매긴 심각도는 폐기했다(트리아지 규율).

**[Decision] 1건**

- [x] [Review][Decision] **상태 거부 400·의사 축 409 문구를 "일정 변경" 의미로 갱신할지** — AC3 은 2.3 문구를 "바이트 그대로 계승"하라고 했으나 구현은 `"완료된 예약은 담당 의사를 바꿀 수 없어요."` → `"…일정을 바꿀 수 없어요."` 로 바꿨다(`services/appointments.py:295-300`). 시각도 바꾸는 경로라 옛 문구는 거짓이 되지만, AC3 문구와는 어긋난다. `epics.md` Epic 7 AC3 은 "바이트" 조건이 없어 **스토리 파일과 epics.md 가 서로 다르다**. 같은 성격으로 의사 축 409 `"…다른 의사를 선택해 주세요."` 도 UI 는 시간 선택을 해제해(=다른 시간을 고르라는 행동) 안내와 동작이 어긋난다. 계약 테스트가 부분 일치(`assert "완료" in detail`)라 어느 쪽이든 초록이다.

**[Patch] 16건**

- [x] [Review][Patch] **CAS 409 를 슬롯 충돌로 오분류** — 세 종류 409(의사 축·환자 축·CAS)를 `err.status === 409` 하나로 처리해, 상태 경합 시 "목록을 새로고침해 주세요"를 다이얼로그 안 슬롯 오류로 띄우고 멀쩡한 셀을 `예약됨`으로 찍는다. 목록도 갱신되지 않아 죽은 행 위에 다이얼로그가 남는다 [frontend/components/reschedule-dialog.tsx:288]
- [x] [Review][Patch] **기본 선택(현재 슬롯)이 점유로 판명돼도 해제·안내가 안 된다** — `selectedIsoRef` 가 `effectiveIso` 가 아니라 `selectedIso` 를 미러해, 손대지 않은 기본 선택은 P8 복구 분기를 한 번도 타지 않는다. 의사를 바꿔 그 슬롯이 taken 이 되면 셀은 회색인데 선택은 남고 버튼도 활성 → 제출해야 409 를 안다. 5.3 에서 이식한 가드가 이 간접 참조로 무력화됐다 [frontend/components/reschedule-dialog.tsx:90-93,145,205-212]
- [x] [Review][Patch] **실패 시 목록 재동기화 계약 상실(2.2 패턴 회귀)** — 삭제된 `runDoctorChange` 는 catch 에서 `setReloadNonce` 로 "실패 = 화면이 stale" 을 처리했는데, 새 다이얼로그는 성공만 부모에 올린다. 다른 직원이 완료 처리한 예약이 목록에 `대기` 로 남아 재시도가 반복된다 [frontend/components/reschedule-dialog.tsx:304 · frontend/app/staff/appointments/page.tsx:328-340]
- [x] [Review][Patch] **환자 축이 선택 해제 판정에서 빠짐** — `patient_taken` 을 `patientBusyMs` 에 저장만 하고 해제 판정은 의사 축 `next.has()` 만 본다. 렌더는 합집합이라 셀은 비활성인데 선택은 남는다 [frontend/components/reschedule-dialog.tsx:205-212]
- [x] [Review][Patch] **제출 중 컨트롤 미잠금 → stale closure 가 최신 선택을 지운다** — A 슬롯 저장 클릭 → 응답 대기 중 B 선택 → A 가 409 → catch 가 B 까지 지운다. `proxy-booking-dialog.tsx:853` 이 정확히 이 이유로 `disabled={… || submitting}` 를 두는데 이식 때 빠졌다 [frontend/components/reschedule-dialog.tsx:279,294 · SlotPicker·두 Select 모두]
- [x] [Review][Patch] **과거 가드가 "실제로 바뀔 때만" 이 아니다** — `payload.reserved_at is not None` 이라, 기존 과거 시각을 그대로 실어 의사만 바꾸면 400 으로 거부된다. AC6 계약 이탈 [backend/app/services/appointments.py:324]
- [x] [Review][Patch] **오늘 지난 예약: 안내도 없고 기본 날짜가 막다른 길** — `currentDayYmd` 는 지난 슬롯을 안 거른 전체 격자로 찾고 `currentSlotIso` 는 거른 `slots` 에서 찾는다. 오늘 09:00 예약을 오후에 열면 안내(`!currentDayYmd`)는 안 뜨는데 선택은 비어 있다. 저녁에 열면 `defaultYmd` 가 폴백을 건너뛰어 빈 격자만 보인다(`proxy-booking-dialog.tsx:138-146` 이 막으려던 상태) [frontend/components/reschedule-dialog.tsx:111-119,121-130,447]
- [x] [Review][Patch] **`update_appointment_schedule` 에 `doctor_id is None` 가드 부재** — 형제 함수 `insert_appointment:274` 는 "게이트 조각의 `a.doctor_id = NULL` 비교는 항상 no-match" 를 이유로 커넥션 전에 `ValueError` 를 올리는데 대체 함수가 안 가져왔다. 스키마상 nullable(라이브 실측 0건이라 현재 도달 불가지만 형제 대칭·2차 방어선) [backend/app/db/appointments.py:403-433]
- [x] [Review][Patch] **프런트 `doctorId` null 시 [변경] 버튼이 조용히 죽는다** — `hasChange` 는 `timeChanged` 만으로 true 인데 `handleSubmit` 첫 줄이 `if (!doctorId) return;` 이라 요청도 오류도 없이 반환한다. 가용성 조회도 `!doctorId` 면 건너뛰어 전 슬롯이 예약 가능처럼 보인다 [frontend/components/reschedule-dialog.tsx:184,263]
- [x] [Review][Patch] **삭제된 심볼·경로를 가리키는 주석 4곳** — `services/appointments.py:239`("재배정은 set_appointment_doctor 담당" — 함수 없음) · `:119`("의사 변경은 reserved_at 을 바꾸지 않아" — 이제 바꾼다) · `tests/test_appointments.py:934`("재배정은 /doctor 경로만 담당" — 404) · `db/availability.py:45`("생성·조회는 None 을 넘긴다" — 조회가 non-None 을 넘긴다). AC9 "참조 0" 위반
- [x] [Review][Patch] **테스트 주석이 실제 가드 순서를 거꾸로 고정** — `"가드는 의사 소속 검증·UPDATE 보다 앞"` 이라 썼지만 실제는 ⑤ 소속 검증 → ⑥ 과거 가드. payload 에 `doctor_id` 가 없어 통과할 뿐이라, 주석이 주장하는 계약은 성립하지 않는다 [backend/tests/test_appointments.py:1112-1114]
- [x] [Review][Patch] **테스트 페이크 arity 검사를 버렸다** — 기존 `lambda doctor_id, start, end:` 를 `lambda *a:` 로 바꿔, `select_taken_slots` 인자가 늘거나 순서가 바뀌어도 통과한다. 같은 파일 docstring 이 "파라미터 전달 검증"을 선언하고 신규 테스트는 인자를 명시해 규율이 파일 안에서 어긋난다 [backend/tests/test_availability.py:123,154]
- [x] [Review][Patch] **`revealField` 의 포커스 이동이 무동작** — 대상 `#reschedule-slot-label` 이 `tabIndex` 없는 `<span>` 이라 `scrollIntoView` 만 되고 `.focus()` 는 조용히 실패한다. 390×844 에서 SR 사용자가 오류로 이동하지 못한다 [frontend/components/reschedule-dialog.tsx:48-53,409]
- [x] [Review][Patch] **스토리 Project Structure Notes 가 실제와 어긋난다** — "백엔드 7 + 테스트 2 + 프런트 2 = 11개, 신규 파일 0" 이라 썼으나 실제는 12파일 + 신규 1(`reschedule-dialog.tsx`). Completion Notes 에 사유는 자진 신고했으나 스펙 본문이 갱신되지 않았다
- [x] [Review][Patch] **File List 수치 오류 3건** — `수정 (10)` 이라 쓰고 11개 나열 · `신규 8건` → 실제 7건 · `page.tsx 526 → 400줄` → 실제 381줄
- [x] [Review][Patch] **브라우저 실측 체크리스트에 근거 없는 `[x]` 2건** — Debug Log 는 "Playwright 미연결이라 수동 확인으로 대체" 라 밝히고 확인 항목을 열거하는데, 그 목록에 없는 `시간 경과(page.clock 2시간 전진)` 와 `409 → red 인라인 + 셀 taken` 까지 체크돼 있다. `page.clock` 은 Playwright 전용이라 수동으로는 불가능하다
- [x] [Review][Patch] **`deferred-work.md` 재평가 미이행** — 스코프 커밋이 "7.1 착수 시 재평가한다 … 라인 번호도 7.1 이후 무효" 라 명시했으나 dev 커밋이 이 파일을 건드리지 않았다. 소멸한 3건(`_UPDATE_APPOINTMENT_DOCTOR` 사본 · `runDoctorChange` 골격 · `DoctorChangeDialog` 추출)이 열린 채 남아 있다

**[Patch] 추가 1건 (브라우저 재확인 중 발견 — 2026-07-29)**

- [x] [Review][Patch] **환자 축으로 막힌 슬롯의 사유가 화면에 없다** — 슬롯 셀은 두 축(의사가 찼다 / 이 환자가 이미 잡았다)을 구분 없이 `예약됨` 으로만 그린다(`slot-picker.tsx` 는 동결이라 축 라벨을 넣을 수 없다). [변경] 다이얼로그는 **의사를 바꾸는 게 주 용도**라 "이 의사는 비었는데 왜 안 되지?" 가 곧바로 발생한다 — 지석님이 재확인 첫 시도에서 그대로 걸렸다(id=22 에서 최현우 선택 → 09:30 이 막힘. 원인은 그 환자의 다른 예약 id=146). 격자 아래 사유 안내 한 줄을 추가하고, **의사를 바꿔도 안 풀린다**는 축의 성질을 명시했다(서버 거부 문구와 주어를 맞춰 "이 환자는"). 동결 컴포넌트 무수정 [frontend/components/reschedule-dialog.tsx — patientBusyLabels]
  - 성격(정직): 7.1 회귀가 **아니다** — FR-15b chore 때부터 환자 예약·대리 예약 화면도 동일하다. 다만 [변경] 다이얼로그에서 도달 빈도가 훨씬 높아 여기서 처리했다. 나머지 두 화면은 done 스토리라 7.1 승인 범위 밖 → `deferred-work.md` 기록.


**[Defer] 5건**

- [x] [Review][Defer] `GET /availability` 의 `exclude_appointment_id` 무검증 — 남의 예약 id 를 넘기면 그 점유가 사전 표시에서 가려진다. 프런트는 항상 자기 id 를 보내고 쓰기 게이트가 최종 방어라 실피해는 "제출 후 409". 서버측 소유권 검증은 별도 작업 [backend/app/routers/availability.py:14-37] — deferred, 도달성 낮음(API 직접 호출 전제)
- [x] [Review][Defer] 환자 축 409 테스트가 제약 이름 확인을 실제로 검증 못 함 — 손제작 `UniqueViolation` 은 `diag.constraint_name` 이 None 이라 폴백 분기만 탄다. `_reject_unique_violation` docstring 과 `deferred-work.md:60` 이 이미 인정한 알려진 한계 [backend/tests/test_appointments.py:1055-1078] — deferred, pre-existing
- [x] [Review][Defer] `revealField` 가 `proxy-booking-dialog.tsx:68` 과 바이트 동일한 두 번째 사본 — 5.4 사본 수렴 규율 대상. 공용 유틸 승격은 정리 작업 [frontend/components/reschedule-dialog.tsx:48-53] — deferred, 부채 순증(회고 액션 #3 대상)
- [x] [Review][Defer] 의사만 바꿔도 `reserved_at` 을 무조건 재작성 — 저장값이 30분 비정렬인 행이면 시각이 최대 29분 이동한다. 006 헤더가 경고한 "앱 밖에서 넣은 행" 전제라 앱 경로로는 도달 불가 [backend/app/db/appointments.py:212-214] — deferred, 도달성 없음(앱 외부 데이터 전제)
- [x] [Review][Defer] 변경 경로에 `ForeignKeyViolation` 핸들러 없음 — 생성 경로는 400 한국어인데 변경은 원시 500. 소속 검증 통과 후 의사 행이 삭제되는 경합 전제 [backend/app/services/appointments.py:327-340] — deferred, 도달성 낮음(참조 데이터는 시드 전용)

**Dismiss 1건** — 빈 의사 목록 안내 소실: 구버전은 현재 의사를 제외해 "바꿀 **다른** 의사가 없다"가 성립했으나 7.1 은 현재 의사를 포함하므로 시드(과당 2명 이상)상 도달 불가. 로드 실패는 별도 오류 + 재시도 경로가 있다.


## Dev Notes

### 🎯 이 스토리의 한 줄 요약

**기존 의사 변경 UPDATE 경로를 `(doctor_id, reserved_at)` 두 컬럼으로 일반화하고, 프런트 다이얼로그에 날짜·슬롯을 얹는다.** 신규 게이트·신규 인덱스·신규 마이그레이션은 **하나도 없다**.

### 왜 이 스토리가 필요한가 (읽고 시작할 것)

2.3이 만든 `/doctor` 경로는 `doctor_id`만 만지고 `reserved_at`은 **명시적으로 동결**했다(`_UPDATE_APPOINTMENT_DOCTOR` 주석: *"status·reserved_at·hospital_department_id 는 건드리지 않는다"*). 그래서 **예약 생성 후 시각을 바꿀 경로가 앱 전체에 0개**다 — 직원은 취소 후 재예약밖에 못 해서 예약 행이 끊기고(새 id) 취소 행이 쌓인다. FR-19는 그 구멍을 닫는다.

### 이미 서 있는 것 — 재사용 대응표 (재발명 금지)

| 필요한 것 | 이미 있는 정본 | 하지 말 것 |
|---|---|---|
| (의사, 슬롯) 충돌 판정 | `db/availability.py` `slot_taken_sql(slot_sql, doctor_sql)` | 새 조각 정의 |
| 자기 행 제외 | 같은 조각의 `%(exclude_appointment_id)s` nullable 파라미터 | 새 파라미터 발명 |
| 환자 축 중복 차단 | `db/migrations/006` 인덱스 (UPDATE 에도 자동 발동) | 앱 게이트로 옮기기 |
| UniqueViolation → 409 매핑 | `services/appointments.py` `_reject_unique_violation` | 두 번째 매핑 작성 |
| CAS(대기·확정 조건부 UPDATE) | `_UPDATE_APPOINTMENT_DOCTOR` 의 `status = any(%(allowed_sources)s)` | 새 가드 |
| 과거 시각 400 문구 | `create_appointment` 의 `"이미 지난 시간이에요…"` | 새 문구 |
| 의사↔진료과 소속 검증 | `_require_doctor_in_department` (문구 2종 포함) | 재구현 |
| 404 문구 | `fetch_appointment_or_404` | 재구현 |
| 30분 슬롯·날짜 선택지 | `lib/booking-slots.ts` (`slotsForSeoulDay`·`seoulDayOptions`·`formatSeoulDayLabel`) | 로컬 사본(5.4가 없앤 것) |
| 슬롯 격자 UI(3상태·a11y) | `components/slot-picker.tsx` — **동결 컴포넌트** | 수정·포크 |
| 두 축 합치기·409 마킹·remount | `components/proxy-booking-dialog.tsx` **참조 구현** | 처음부터 설계 |

### 백엔드 배선 청사진

**`_UPDATE_APPOINTMENT_SCHEDULE` — 한 문장 안 3층 가드**

```
① CAS      status = any(%(allowed_sources)s)        0행 → None → 409 (CAS_CONFLICT_DETAIL)
② 의사 축   not slot_taken(새 의사, 새 슬롯,          SlotTakenError → 409 슬롯 문구
             exclude_appointment_id = 자기 id)
③ 환자 축   006 인덱스가 UPDATE 시 자동 발동          UniqueViolation → _reject_unique_violation → 409
```

2.3 문에서 바뀌는 곳은 **두 줄뿐**이다:
- `set doctor_id = %(doctor_id)s` → `set doctor_id = %(doctor_id)s, reserved_at = %(reserved_at)s`
- `slot_taken_sql("(select reserved_at from target)")` → `slot_taken_sql("%(reserved_at)s")`

⚠️ 두 번째가 **핵심**이다. 2.3은 슬롯을 대상 행에서 유도했다(시각이 안 바뀌니까). 7.1은 **요청된 새 시각**으로 판정해야 한다 — 안 바꾸면 새 시각의 충돌을 검사하지 않고 옛 시각만 본다.
⚠️ `target` CTE 는 **삭제하지 말 것** — `cas_ok` 플래그가 `_interpret_*_row` 의 "CAS 우선" 규칙에 쓰인다.

**서비스 유효값 합성** — SQL 분기를 만들지 않기 위해 서비스가 먼저 합성한다:
```python
effective_doctor_id = payload.doctor_id if payload.doctor_id is not None else current["doctor_id"]
effective_slot = to_slot(payload.reserved_at if payload.reserved_at is not None else current["reserved_at"])
```
UPDATE 는 항상 두 컬럼을 쓴다(같은 값을 다시 써도 무해).

⚠️ **`to_slot()` 을 반드시 통과시킬 것** — 원시 payload 를 그대로 쓰면 003 의 `reserved_at` 30분 CHECK 를 위반해 원시 DB 에러가 난다(2.1이 겪은 함정). 이미 정렬된 값을 다시 floor 하는 건 무해한 no-op 이고, 저장 형태에 의존하지 않는 게 AD-3 규칙이다.

**무변경 판정(AC2)** — 비교는 **정규화 후**에 한다. 원시 payload 와 DB 값을 직접 비교하면 tz 표기·마이크로초 차이로 "안 바뀌었는데 바뀐 것"이 된다:
```python
unchanged = (effective_doctor_id == current["doctor_id"]
             and effective_slot == to_slot(current["reserved_at"]))
```
거부 문구(UX-DR10 — 정직·실행 가능, 직원 톤): `"바뀐 내용이 없어요. 담당 의사나 진료 시간을 바꿔 주세요."`

### 함정 5건 (실측 기반 — 여기서 넘어진다)

1. **과거 가드를 무조건 걸면 2.3 회귀** — `test_change_doctor_past_appointment_still_allowed` 가 "과거 예약의 의사 변경은 허용"을 고정하고 있다. 가드는 **`payload.reserved_at is not None` 일 때만** 적용한다.
2. **`/availability` 환자 축엔 제외 조건이 아예 없다** — `_SELECT_PATIENT_TAKEN_SLOTS` 를 보면 `exclude_appointment_id` 파라미터 자체가 없다. 의사 축(`occupied_sources_sql`)만 고치면 **자기 슬롯이 여전히 patient_taken 으로 와서** 화면이 막힌다. 두 쿼리 **모두** 고칠 것.
3. **의사 Select 의 "현재 의사 제외" 필터를 지워야 한다** — 현재 `doctorCandidates` 가 `d.id !== doctorTarget.doctor_id` 로 거른다. 그대로 두면 "의사 유지 + 시각만 변경"이 UI 에서 불가능하다(AC2 위반).
4. **`extra="forbid"` 는 유지하되 필수는 풀린다** — `doctor_id` 가 optional 이 되므로 `test_change_doctor_missing_doctor_id_returns_422` 의 계약이 바뀐다(422 → 이제 `reserved_at` 도 없으면 **서비스 400**). 테스트를 지우지 말고 **의도를 바꿔 재작성**한다.
5. **`slot-picker` 는 `takenMs` 미지정 시 루트 구조가 달라진다** — 범례가 없는 2상태 렌더다. 변경 다이얼로그는 항상 `takenMs`(=`unavailableMs`)를 넘겨 3상태 + 범례로 쓴다.
6. **409 셀 마킹은 의사 축(`takenMs`)에 넣는다 — 축을 가리려 들지 말 것.** 어느 축에서 온 409든 렌더는 `unavailableMs = takenMs ∪ patientBusyMs` 합집합을 쓰므로 결과가 같고, 이어지는 재조회가 두 축을 서버 진실로 덮는다. `chore/patient-slot-guard` 코드리뷰에서 환자 축 마킹으로 바꿨다가 **되돌린 이력**이 있다(응답이 두 축을 함께 주므로 무한 재시도가 애초에 안 생긴다). 같은 판단을 다시 하지 말 것.

### 프런트 배선 — `proxy-booking-dialog.tsx` 대응표

`[변경]` 다이얼로그는 대리 예약 다이얼로그의 **축소판**이다(환자 검색·진료과 선택이 빠짐 — 예약이 이미 둘 다 갖고 있다).

| 대리 예약(정본) | `[변경]` 다이얼로그 |
|---|---|
| `AUTO_DOCTOR` 자동 배정 옵션 | **없음** — 변경은 항상 명시적 의사(자동 배정은 생성 전용) |
| 환자 검색 → `patient` | `target.patient_id` 고정 |
| 진료과 Select → `deptId` | `target.hospital_department_id` 고정(과 이동 스코프 밖) |
| `seoulDayOptions(new Date(), 7)` + `defaultYmd` | **그대로** |
| `slots` = `slotsForSeoulDay(ymd).filter(미래)` | **그대로** |
| `unavailableMs = takenMs ∪ patientBusyMs` | **그대로** (단 자기 행 제외된 값) |
| `handleDateChange` 동일값 재발화 가드 | **그대로** (base-ui Select 함정) |
| 409 → red 인라인 + 셀 taken + 선택 해제 + nonce | **그대로** |
| `submittingRef` 동기 재진입 가드 | **그대로** |
| 열 때 `key` 로 remount | **그대로** |

**기본 선택값:** 다이얼로그를 열면 `doctor` = 현재 담당, `날짜/시각` = 현재 `reserved_at` 이 기본 선택돼 있어야 한다 — 직원이 "무엇을 바꾸는지" 보이는 상태에서 시작한다. 현재 슬롯은 AC7 덕분에 `taken` 이 아니라 **선택됨**으로 렌더된다.
⚠️ 현재 예약이 **과거**거나 7일 창 밖이면 그 슬롯이 `slots` 목록에 없다 — 이때는 날짜/시각 미선택으로 시작하고, 의사만 바꿔 저장하는 경로(AC2·AC6)가 살아있어야 한다.

### 건드리지 않는 것 (동결 유지)

`backend/app/main.py` · `config.py` · `db/pool.py` · `app/slots.py`(`to_slot`·`ensure_utc` 계약) · `routers/services/db/refdata.py` · `services/medical_records.py` · `db/medical_records.py` · `db/migrations/*` · `db/seed/*` · `POST /appointments` 전 경로(생성·자동 배정) · `PATCH /appointments/{id}`(status) · `frontend/lib/api.ts` 의 `request`·기타 함수 · `components/slot-picker.tsx` · `lib/booking-slots.ts` · `lib/format.ts` · `lib/category-color.ts` · `components/category-badge.tsx` · `components/appointment-status-badge.tsx` · `components/role-context-bar.tsx` · `components/proxy-booking-dialog.tsx` · `components/ui/*`

### 핵심 안티패턴 (하지 말 것)

- ❌ **`slot_taken_sql`·`free_doctor_sql`·`occupied_sources_sql` 새 조각 정의** — 조각은 `db/availability.py` 한 벌(AD-3). 호출부는 **6곳 유지**(교체지 추가가 아님 — `.claude/rules/backend.md` 트립와이어).
- ❌ **SQL 조각 빌더에 런타임 유도 값 전달** — 인자는 반드시 **소스 리터럴**(`"%(reserved_at)s"` 같은). 요청 파라미터를 f-string 으로 만들어 넘기면 그 즉시 충돌 게이트에 SQL 구조 주입이다.
- ❌ **환자 축을 앱 게이트로 옮기기** — 006 인덱스를 지우면 그 축의 TOCTOU 차단이 함께 사라진다(AD-4 명시).
- ❌ **`PATCH /appointments/{id}`(status)에 `reserved_at` 섞기** — status 소유권(AD-5)과 분리 유지. `extra="forbid"` 가 이를 고정하고 테스트가 지킨다.
- ❌ **`hospital_department_id` 변경 / 다른 과 의사 허용** — 과 이동은 스코프 밖(2.3과 동일 경계). 후보는 항상 그 예약의 진료과 안.
- ❌ **자동 배정(`doctor_id: null`)을 변경 경로에 도입** — 생성 전용(5.2). 변경에서 `doctor_id` 미지정은 "자동"이 아니라 **"현재 의사 유지"** 다.
- ❌ **`slot-picker.tsx` 수정·포크** — 동결. 필요한 건 전부 props 로 있다.
- ❌ **`lib/booking-slots.ts` 로컬 사본** — 5.4가 정확히 이걸 없앴다.
- ❌ **환자 화면·의사 화면에 변경 노출** — 직원 예약 관리 전용(FR-19 경계).
- ❌ **변경 이력 테이블·감사 로그 추가** — PRD §4 제외 항목. `reserved_at` 은 제자리에서 덮어쓴다.

### 테스트 이관 표 (`/doctor` 폐기 — 15건)

`tests/test_appointments.py` 2.3 블록. **삭제가 아니라 이관**이다 — 경로·페이로드만 바꾸고 의도는 보존한다.

| 기존 테스트 | 7.1 처리 |
|---|---|
| `test_change_doctor_on_pending_appointment` | 경로만 `/reschedule` |
| `test_change_doctor_on_confirmed_appointment` | 경로만 |
| `test_change_doctor_unknown_appointment_returns_404` | 경로만 |
| `test_change_doctor_completed_rejected` | 경로만(문구 불변) |
| `test_change_doctor_cancelled_rejected` | 경로만(문구 불변) |
| `test_change_doctor_unknown_doctor_rejected` | 경로만 |
| `test_change_doctor_wrong_department_rejected` | 경로만 |
| `test_change_doctor_same_doctor_rejected` | **의도 변경** — 같은 의사만 보내면 이제 "무변경 400". 시각을 함께 보내면 **성립**해야 한다(신규 케이스 추가) |
| `test_change_doctor_race_returns_409` | 경로만 |
| `test_change_doctor_missing_doctor_id_returns_422` | **의도 변경** — 둘 다 없으면 서비스 400(422 아님) |
| `test_change_doctor_rejects_extra_status_field` | 경로만(`extra="forbid"` 유지) |
| `test_change_doctor_slot_conflict_returns_409_with_doctor_message` | 경로만 |
| `test_change_doctor_past_appointment_still_allowed` | **회귀 가드 — 반드시 살린다.** 시각 미지정 시 과거 예약의 의사 변경이 여전히 통과해야 함(함정 1) |
| `test_doctor_update_interpretation_prefers_cas_over_slot_conflict` | 함수명만 `_interpret_schedule_update_row` |
| `test_doctor_update_interpretation_slot_conflict_and_success_paths` | 함수명만 |

**유지(무수정):** `test_patch_status_rejects_extra_doctor_id_field` — status 엔드포인트의 분리 계약이라 7.1과 무관.

**신규 최소 세트:** 시각만 변경 성립 · 의사+시각 동시 변경 성립 · 무변경 400 · 새 시각 과거 400 · 환자 축 `UniqueViolation` 409 · 자기 현재 슬롯 유지 성립(자기 행 제외 증명) · `/availability` `exclude_appointment_id` 두 축 전달 · 미지정 시 기존 인자 동일.

**전체 카운트:** 현재 131건(`test_appointments` 59). 이관 후 순증은 신규 8건 내외.

### 테스트 전략 · 한계 (정직)

계약 테스트는 **db 계층을 monkeypatch** 한다 — 실 SQL·실 CTE·실 006 인덱스를 실행하지 않는다. 즉 `_UPDATE_APPOINTMENT_SCHEDULE` 의 `reserved_at` 반영, 자기 행 제외, 인덱스 발동은 **pytest 로 증명되지 않는다.** 실 보증은 아래 curl 실증과 브라우저 실측이 담당한다(`.claude/rules/workflow.md`).

### curl 실증 시나리오 (로컬 uvicorn + 실 Supabase, 6종)

1. **시각만 변경** — 대기 예약의 `reserved_at` 만 미래 빈 슬롯으로 → 200, `doctor_id` 불변, `status` 불변
2. **의사만 변경** — `doctor_id` 만 → 200, `reserved_at` 불변 (2.3 회귀 없음)
3. **둘 다 변경** — 200, 두 필드 모두 반영
4. **의사 축 409** — 다른 예약이 점유한 (의사, 슬롯)으로 → 409 `"이 시간엔 선택한 의사의 예약이 이미 있어요…"`
5. **환자 축 409** — 그 환자가 이미 잡은 다른 슬롯으로 → 409 `"이 환자는 그 시간에 이미 다른 예약이 있어요…"` (**006 인덱스가 UPDATE 에서 발동함을 증명**)
6. **자기 슬롯 유지** — `reserved_at` 을 **현재 값 그대로** + 의사만 변경 → 200(자기 행 제외 증명 — 제외가 없으면 여기서 409 가 난다)

추가: `GET /availability?…&exclude_appointment_id=<id>` 가 그 예약의 슬롯을 `taken`·`patient_taken` **양쪽에서** 빼는지 확인.

### 브라우저 실측 체크리스트 (390×844, Epic 6 회고 액션 #1)

- [x] 대기·확정 행에 `[변경]` 1개(버튼 3개 유지), 완료·취소 행은 불변
- [x] 다이얼로그 기본값 = 현재 의사·현재 날짜·현재 시각(선택됨 상태)
- [x] **현재 슬롯이 `예약됨`으로 안 보인다**(AC7 — 이게 자기 행 제외의 눈으로 보는 증거)
- [ ] **시간 경과**: `page.clock` 으로 2시간 전진 후 열기 → 지난 슬롯이 목록에서 빠짐 — ⚠️ **미수행**(Playwright 미연결, `page.clock` 은 Playwright 전용이라 수동 대체 불가). 남은 리스크: 다이얼로그를 오래 열어둔 뒤 지난 슬롯을 고르는 경로. 제출 직전 재검증 + 서버 400 이 방어층이나 육안 확인은 없다
- [x] **같은 값 재선택**: 같은 날짜·같은 의사 다시 고르기 → 선택·가용성이 이유 없이 초기화되지 않음
- [x] 다른 예약이 점유한 슬롯 = `예약됨`(비활성), 클릭 불가
- [ ] 409 → red 인라인 + 그 셀 즉시 `예약됨` + 다이얼로그 유지(입력 보존) — ⚠️ **브라우저 미수행**(수동 확인 목록 밖). 서버 계약은 curl 실증 #4·#5 로 확인됨
- [x] 콘솔 0 · 가로 오버플로 0

### 검증 데이터 원복 규율

이 스토리는 **예약을 새로 만들지 않고 기존 행을 옮긴다** — 원복은 삭제가 아니라 **원래 `doctor_id`·`reserved_at` 로 되돌리는 UPDATE** 다. 실증 전에 대상 행의 원값을 반드시 적어 둘 것.
충돌 시나리오(4·5)를 위해 예약을 새로 만들었다면 처방 → 기록 → 예약 순으로 삭제(공용 라이브 DB — 시드 재실행 절대 금지).

### Project Structure Notes

건드리는 파일은 **백엔드 7 + 테스트 2 + 프런트 3 = 12개**(그중 신규 1). 신규 디렉토리 0 · 신규 의존성 0 · 마이그레이션 0.

> **신규 파일 1건 — 계획 수정(2026-07-29 dev).** 원안은 "신규 파일 0" 이었으나 `frontend/components/reschedule-dialog.tsx` 를 신설했다. 사유: ① 열 때 `key` remount 로 시각을 재계산하는 구조가 컴포넌트 경계 없이는 성립하지 않는다(`proxy-booking-dialog` 가 같은 이유로 분리돼 있다) ② 페이지에 상태 10개 + effect 2개를 더 얹으면 ~800줄이 된다. 결과적으로 `page.tsx` 는 526 → 381줄로 줄었고, `deferred-work.md` 의 "DoctorChangeDialog 컴포넌트 추출"(2-3 리뷰 defer)이 함께 해소됐다.

```
backend/app/routers/appointments.py      /doctor 삭제 → /reschedule
backend/app/routers/availability.py      exclude_appointment_id 파라미터
backend/app/services/appointments.py     set_appointment_doctor → set_appointment_schedule
backend/app/services/availability.py     파라미터 통과
backend/app/schemas/appointments.py      AppointmentDoctorUpdate → AppointmentRescheduleUpdate
backend/app/db/appointments.py           _UPDATE_APPOINTMENT_SCHEDULE
backend/app/db/availability.py           두 쿼리에 exclude 반영
backend/tests/test_appointments.py       2.3 블록 이관
backend/tests/test_availability.py       exclude 케이스
frontend/lib/api.ts                      rescheduleAppointment · getAvailability 인자
frontend/app/staff/appointments/page.tsx [변경] 다이얼로그
```

### 이전 스토리 인텔리전스

- **2.3** — `/doctor` 서브리소스 분리 근거(status 소유권), `_require_doctor_in_department` 문구 정본. 이 스토리가 그 결정을 **의도적으로 뒤집는다**(경로 통합) — 근거는 제안서 §3.4.
- **5.1** — 게이트 조각·`exclude_appointment_id`·과거 시각 가드가 전부 여기서 왔다. "사전 표시는 예방, 최종 차단은 서버" 규율.
- **5.3** — `proxy-booking-dialog` 의 두 축 합치기·remount·409 마킹이 이 스토리의 참조 구현. 특히 *"후보 필터는 두 축 모두 본다"* 는 코드리뷰 High 교훈.
- **5.4** — 사본 수렴 규율. 새 로컬 헬퍼를 만들면 그때 없앤 부채가 되돌아온다.
- **chore/patient-slot-guard** — `_reject_unique_violation` 의 제약 이름 확인이 왜 필요한지(이름 미확인 매핑은 의사 충돌을 환자 문구로 오보). 그 함수를 **그대로** 재사용할 것.
- **Epic 6 회고 #1·#2** — 폼·선택 위젯 스토리는 "시간 경과 · 같은 값 재선택"을 실측에 필수 포함, 컴포넌트 수명은 설계 결정으로 명시(이 스토리는 **열 때 remount** 로 결정).

### 최신 기술 정보 (고정 버전 — 이것이 계약)

| 이름 | 버전 | 7.1 관련 |
|---|---|---|
| Next.js (App Router) | 16.2.10 | `frontend/AGENTS.md` 필독 — 훈련 데이터와 다른 버전. effect 내 동기 setState 금지(`setTimeout(…,0)`·promise 콜백 패턴) |
| React | 19 | `react-hooks/purity` 린트가 `Date.now()` 를 막는다 → `new Date().getTime()` 사용(booking-slots 선례) |
| base-ui Select | 현행 | ① `Root` 에 `items` 를 넘겨야 `SelectValue` 라벨이 렌더된다 ② **같은 값 재선택도 `onValueChange` 발화** — 동일값 가드 필수(6.3 데드락 재현 이력) |
| FastAPI / Pydantic v2 | 현행 | `extra="forbid"` 유지. 필수 누락 422 는 리스트 detail 이라 `lib/api.ts` 가 일반 메시지로 바꾼다 → 도메인 거부는 **서비스 400 문자열** |
| psycopg 3 | 현행 | named 파라미터(`%(name)s`) 통일 — 한 문장 안 positional 혼용 금지 |
| Python | 3.13 (uv 경유) | `python3 -m pytest` 는 실패(시스템 3.11·pytest 없음). 명령은 반드시 `backend/` 안에서 |

### References

- FR-19 · Epic 7 · Story 7.1 AC: `planning-artifacts/epics.md`
- 승인된 변경 제안(사유·트레이드오프·미이행 목록): `planning-artifacts/sprint-change-proposal-2026-07-29.md`
- FR-19 PRD 항목: `planning-artifacts/prds/prd-hospital-care-2026-07-12/prd.md` §F3
- AD-3(슬롯 정규화) · **AD-4 일정 변경 조항** · AD-5(status 소유) · AD-10(API 계약): `planning-artifacts/architecture/architecture-hospital-care-2026-07-13/ARCHITECTURE-SPINE.md`
- UX-DR3(슬롯 피커) · UX-DR6(Dialog) · UX-DR7(도메인 거부) · UX-DR11(반응형): `planning-artifacts/ux-designs/ux-hospital-care-2026-07-13/DESIGN.md`
- IA 행 액션: `planning-artifacts/ux-designs/ux-hospital-care-2026-07-13/EXPERIENCE.md`
- 006 인덱스 경계·선행 조건: `db/migrations/006_uq_patient_slot.sql` 헤더
- SQL 조각 빌더 안전 관례·트립와이어: `.claude/rules/backend.md`
- 사이클·done 정의·릴리스 게이트: `.claude/rules/workflow.md`
- 2.3 결정 기록(뒤집는 대상): `implementation-artifacts/2-3-직원-담당-의사-변경-재배정.md`
- 참조 구현: `frontend/components/proxy-booking-dialog.tsx`

## Dev Agent Record

### Agent Model Used

claude-opus-5 (Claude Code, `/bmad-dev-story`)

### Debug Log References

**TDD 사이클** — red 27 실패 → green 140 통과(기존 131, 순증 9).

```
pytest -q         → 140 passed
ruff check .      → All checks passed!
npm run lint      → 0
npm run build     → 통과 (16 라우트, 타입 오류 0)
```

**curl 실증** (로컬 uvicorn + 실 Supabase, 검증 예약 137·138·139·140):

| # | 시나리오 | 결과 |
|---|---|---|
| 1 | 시각만 변경 (139: doc3 유지 → 05:00Z) | `200` doc 불변 ✓ |
| 2 | 의사만 변경 (137: → doc4) | `200` reserved_at 불변 ✓ |
| 3 | 둘 다 변경 (139: doc4 + 06:00Z) | `200` 두 필드 반영 ✓ |
| 4 | 의사 축 409 (139 → doc4 @04:00Z, 140 점유) | `409` "이 시간엔 선택한 의사의 예약이 이미 있어요…" ✓ |
| 5 | **환자 축 409** (139 → doc4 @00:30Z, 138 점유) | `409` "이 환자는 그 시간에 이미 다른 예약이 있어요…" ✓ |
| 6 | **`/availability` 자기 행 제외** (138 = p1·doc3·00:30) | 제외 없음 `taken=[00:30,03:00] patient_taken=[00:30,03:00,06:00]` → `exclude=138` 시 `taken=[03:00] patient_taken=[03:00,06:00]` — **두 축 모두에서 00:30 제거** ✓ |
| 7 | 무변경 / 빈 바디 / 과거 시각 | `400` × 3, 문구 각각 구분 ✓ |
| 8 | `status` 동봉 / 옛 `PATCH …/doctor` | `422` / `404` ✓ |

**#5 가 이 스토리의 핵심 실증이다** — 006 부분 유니크 인덱스가 INSERT 전용이 아니라 **UPDATE 에서도 발동**함을 라이브에서 확인했다. 계약 테스트는 db 를 monkeypatch 하므로 이건 pytest 로 증명할 수 없다.

**브라우저 실측** — 지석님이 `localhost:3000/staff/appointments` 에서 직접 확인(2026-07-29). Playwright·Chrome 확장이 이 환경에 미연결이라 에이전트 자동 실측 대신 수동 확인으로 대체. 확인 항목: 행 액션 `[변경]` 3개 · 다이얼로그 기본값(현재 의사·날짜·시각) · **현재 슬롯이 `예약됨` 아님(AC7 육안 증거)** · 의사 축(12:00)·환자 축(15:00) 두 칸 `예약됨` · 시각만 변경 반영 · 같은 값 재선택 무손실 · 무변경 시 버튼 비활성 · 390×844 오버플로 0 · 콘솔 0.

**검증 데이터 원복** — `docs/환경셋업.md` §5 ① 경로(uv + psycopg)로 예약 137·138·139·140 삭제. 처방·기록은 0건(생성 안 함). 원복 후 예약 총 4건 · 활성 1건(id=22) = 검증 전과 동일.

### Completion Notes List

- **AC1~AC9 전부 충족.** 마이그레이션 0건 · 신규 의존성 0건 · DB 스키마 변경 0.
- **`PATCH /appointments/{id}/doctor` 폐기 완결** — 라우터·서비스·db SQL·스키마·`lib/api.ts` 함수·계약 테스트 15건이 함께 이동했고, 옛 경로는 `404`. 살아있는 코드·규칙 파일의 잔재 0(`update_appointment_doctor`·`AppointmentDoctorUpdate`·`_UPDATE_APPOINTMENT_DOCTOR`·`_interpret_doctor_update_row` 전부 갱신). 완료 스토리 파일(2-3 등)의 언급은 시점 기록물이라 보존.
- **SQL 조각 트립와이어 6곳 유지** — `_UPDATE_APPOINTMENT_DOCTOR` → `_UPDATE_APPOINTMENT_SCHEDULE` 는 **교체**라 호출자가 늘지 않았다(`.claude/rules/backend.md` 서술과 실측 일치).
- **스토리 계획에서 벗어난 것 1건(정직):** 스토리는 "신규 파일 0"이었으나 **`frontend/components/reschedule-dialog.tsx` 를 신설**했다. 사유: ① 열 때 `key` remount 로 시각을 재계산하는 구조가 컴포넌트 경계 없이는 성립하지 않는다(`proxy-booking-dialog` 가 같은 이유로 분리돼 있다) ② 페이지에 상태 10개 + effect 2개를 더 얹으면 ~800줄이 된다. 결과적으로 `page.tsx` 는 526 → 400줄로 **줄었다**. `deferred-work.md` 의 "DoctorChangeDialog 컴포넌트 추출"(2-3 리뷰 defer)이 이 추출로 자연 해소됐다.
- **2.3 계약 변화 2건** — ① `"이미 담당하고 있는 의사예요"` 400 규칙 **제거**(의사 유지 + 시각 변경이 정당한 요청이 됨) → 무변경 400 으로 대체 ② `doctor_id` 필수 422 → 둘 다 선택이라 빈 요청은 **서비스 400**(422 리스트 detail 은 `lib/api.ts` 가 일반 메시지로 바꿔 직원이 이유를 못 본다).
- **회귀 가드 유지** — `test_reschedule_past_appointment_doctor_only_still_allowed` 가 "과거 예약의 의사만 변경"을 계속 고정한다. 과거 가드를 무조건 걸면 이 테스트가 깨진다(스토리 함정 1).
- **실측으로 드러난 것(스토리 예측 적중)** — `_SELECT_PATIENT_TAKEN_SLOTS` 에는 제외 조건이 **아예 없었다**. 의사 축만 고쳤다면 자기 예약이 `patient_taken` 으로 남아 화면이 계속 막혔을 것이다.
- **정직한 한계** — 계약 테스트는 db 를 monkeypatch 하므로 `_UPDATE_APPOINTMENT_SCHEDULE` 의 `reserved_at` 반영·자기 행 제외·006 인덱스 발동을 증명하지 못한다. 그 셋의 실 보증은 curl 실증 #3·#5·#6 이 담당한다.

### File List

**수정 (11)**
- `backend/app/routers/appointments.py` — `/doctor` 삭제 → `/reschedule` 신설
- `backend/app/routers/availability.py` — `exclude_appointment_id` 선택 파라미터
- `backend/app/services/appointments.py` — `set_appointment_schedule`(유효값 합성 + 가드 7단계)
- `backend/app/services/availability.py` — `exclude_appointment_id` 두 축 통과
- `backend/app/schemas/appointments.py` — `AppointmentRescheduleUpdate`(두 필드 선택)
- `backend/app/db/appointments.py` — `_UPDATE_APPOINTMENT_SCHEDULE` · `update_appointment_schedule` · `_interpret_schedule_update_row`
- `backend/app/db/availability.py` — 환자 축 쿼리에 제외 조건 신설, 두 select 에 인자
- `backend/tests/test_appointments.py` — 2.3 블록 이관 15건 + 신규 7건(리뷰 반영으로 +3 → 10건)
- `backend/tests/test_availability.py` — 제외 케이스 2건 + 기존 더블 시그니처 갱신
- `frontend/lib/api.ts` — `rescheduleAppointment` · `getAvailability` 5번째 인자
- `frontend/app/staff/appointments/page.tsx` — `[변경]` 배선, 의사 변경 다이얼로그 제거(526 → 387줄)

**신규 (1)**
- `frontend/components/reschedule-dialog.tsx` — 일정 변경 다이얼로그(사유는 Completion Notes 참조)

**추적 (2)**
- `_bmad-output/implementation-artifacts/7-1-예약-일정-변경.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`

## Change Log

| 날짜 | 작성 | 내용 |
|---|---|---|
| 2026-07-29 | create-story | 스토리 파일 생성 — FR-19/Epic 7(correct-course 2026-07-29 승인) 기반, AC 9건·Task 11건. backlog → ready-for-dev |
| 2026-07-29 | 릴리스 | PR #29 머지(512c7b1) → Railway·Vercel 배포 → 라이브 실측 6종 + 번들 확인 → 검증 데이터 원복. **done 3게이트 충족** → done |
| 2026-07-29 | code-review | 4층 리뷰(Codex+3층) 42건 → 22건 트리아지. Decision 1·Patch 16 전건 반영 + 브라우저 재확인 중 발견 1건 추가 반영, Defer 5 기록. pytest 140→143 · 라이브 curl 5종 재실증 |
| 2026-07-29 | dev-story | 구현 완료 — `PATCH …/doctor` → `…/reschedule` 교체, `/availability` 자기 행 제외(두 축), `[변경]` 통합 다이얼로그 신설. pytest 131→140·ruff 0·lint 0·build 통과, curl 실증 8종, 브라우저 실측(사용자 확인), 검증 데이터 원복. in-progress → review |
