---
topic: 병원 진료관리 풀스택 서비스 (hospital-care) PRD
updated: 2026-07-13T15:58
---

- (decision) 진행 방식=Coaching path 선택 (사용자가 학습 목적으로 함께 작성 선호). 스테이크=교육 과제/1인/목요일 마감 → hobby-solo 수준 rigor, ~2p PRD 목표.
- (decision) 시작점=Journey-led. 사용자 여정(환자/직원)으로 섹션 순서 구성, 여정에서 FR 추출.
- (decision) 범위 축소: 단일 병원 전제. 예약 흐름에서 '병원 선택' 단계 제거(진료과 선택은 유지). hospital 테이블은 1개 시드로만 존재, medical_record는 여전히 발생 병원·과를 자체 보관.
- (decision) 환자 신원=목록에서 본인 이름 선택 방식 확정(로그인 대체). 추후 수정 가능하다고 사용자 합의.
- (decision) UJ-1, UJ-2 두 여정 및 주인공(이수민/박서준/김민재) 승인. 이 여정에서 FR 추출 진행.
- (decision) 참조 데이터(진료과·의사·약)=시드 데이터로만. 관리/편집 화면은 이번 범위 제외(여유 시 확장).
- (decision) FR-1~FR-14 (6개 기능 그룹) 사용자 승인. 추가/삭제 없이 확정.
- (change) 보안 방향 변경(브리프 'RLS off' 가정 대체): 모든 테이블 RLS ON + anon 키 DB 직접 접근 차단, 모든 접근은 FastAPI 서버측 키 경유. 로그인은 여전히 없음(역할 선택). 환자 격리는 API 레벨 필터. DB 레벨 사용자별 격리(진짜 격리)는 확장 과제.
- (decision) 예약 중복(가용성) 검증 포함. 가용성 단위=(의사,시각): 한 의사·한 시각에 활성 예약/walk-in 하나만(FR-15).
- (decision) 예약 시 의사 미선택이면 해당 진료과의 그 시각 빈 의사를 자동 배정, 없으면 예약 불가(FR-6 개정). 결과적으로 예약도 의사 배정 → medical_record.doctor_id NOT NULL과 정합.
- (decision) walk-in 포함(FR-16): 예약 없이 빈 의사에 즉시 진료·기록(appointment_id 없이), 같은 (의사,시각) 슬롯 점유. 영업시간/근무표 검증은 범위 밖. 슬롯=정확 시각 일치(MVP), 겹침계산은 확장.
- (change) 리뷰 반영: (1)슬롯=30분격자로 충돌규칙 정정-정확시각일치 폐기 (2)FR-6 자기모순 해소:예약은 항상 의사 배정(선택 or 자동), FR-7=확정/취소/재배정, 브리프 '의사는 진료 시 배정'은 대체됨 (3)walk-in 빈의사 없으면 거부 (4)범위 P0/P1 분리로 가드레일 준수 (5)FR-8 완료전이=진료기록 작성시 (6)addendum A4 교차테이블 앱레벨 검사로 정정+부분유니크인덱스 한계 명시.
- (change) 브리프 대조 갭 반영: 문제(왜) 프레이밍 §1 추가, 정량페인 미조사 caveat §7 추가, 정규화 스키마 유지 이유 addendum A3 추가, schema.sql에 RLS 구문 없음 경고 A1 추가, medical_record.appointment_id UNIQUE 부재 A3 명시.
- (event) PRD finalized
- (event) PRD 검증(validate) 실행: 등급 Good. 루브릭 7차원 strong/adequate(문서 품질 high 0). 적대적+데이터모델 리뷰어가 구현 정합성 high 4건 발견(RLS 미구현·예약↔기록 1:1 미강제·의사슬롯 유일성 DB 미강제·P0가 FR-6 항상배정 위반). validation-report.html/.md 생성.
- (decision) 검증 반영 Update: P0 의사배정=직접 선택 필수(자동배정은 P1). FR-6·UJ-1 step2·§4 P0에 (P0)/(P1) 태그. → P0에서 doctor_id 항상 채워져 예약→기록 E2E가 P0만으로 관통(High1 해소).
- (change) 예약:기록 1:1 DB 강제(High3): medical_record(appointment_id) where not null 부분 유니크 인덱스 추가 결정(A3). FR-8 완료전이=기록 저장과 같은 트랜잭션 부작용으로 명시, walk-in(appointment_id null)은 전이 대상 제외.
- (change) 가용성 강제수준 정직 명시(High4): FR-15/A4에 '앱레벨·단일세션 전제에서만 차단 보장(TOCTOU 범위 밖)' 추가. reserved_at 30분 정렬 CHECK + 충돌검사·삽입 단일 트랜잭션 결정. 완전강제(EXCLUDE/슬롯뷰)는 확장.
- (change) RLS를 P0 초기 산출물로 승격(High2): NFR-4/A1에 완료정의(배포 전 Supabase advisor로 9테이블 RLS ON 확인) 추가. + FR-2에 '기밀 격리 아님' caveat, FR-16 walk-in 진료과 먼저 선택 명시+‘대기 처리’ 제거(거부로 종료).
- (event) Update 완료: High 4 + medium 5 반영. prd.md/addendum.md updated=2026-07-13.
- (decision) [아키텍처 반영 2026-07-13] NFR-1 배포 문구 갱신: 'Railway 단일' → '프런트 Vercel · 백엔드(FastAPI) Railway'. 사용자 결정. 요약·성공지표·NFR-1·addendum A2 동시 반영. 상세는 architecture 스파인 운영봉투.
