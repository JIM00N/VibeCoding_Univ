# PRD — 계모임 (gyemoim)

| 항목 | 값 |
|---|---|
| 작성일 | 2026-08-19 |
| 유형 | 4시간 타임박스 프로토타입 (데모용) |
| 참조 서비스 | 소모임(somoim.co.kr) — 정보구조·레이아웃 참조. 이름·로고는 독자 브랜딩 |
| 라이브 | Vercel (main push = 자동 배포) |

---

## 1. 개요

취미 모임을 **둘러보고 · 가입하고 · 정모에 참석하는** 웹 서비스.

발표 자리에서 청중에게 공개 URL을 공유하면, 청중이 자기 폰으로 들어와 임시계정으로 로그인하고
방금 만들어진 모임에 실제로 가입하는 것 — 이것이 이 제품의 완성 상태다.

### 1.1 목표

- **G1** 데모 시나리오(§3) 6단계를 라이브 URL에서 끊김 없이 완주한다.
- **G2** 청중 다수가 동시에 접속해 로그인·가입해도 동작한다.
- **G3** 4시간 안에 끝낸다. 시간이 목표를 이긴다 — 못 끝낼 것 같으면 기능을 자른다(§7).

### 1.2 비목표 (명시적으로 안 한다)

실제 회원가입 · 비밀번호 관리 · 이미지 업로드 · 댓글/게시판 · 채팅 · 알림 · 결제 ·
지도 · 추천 알고리즘 · 연령 설정 · "최근 본 모임" · 앱 다운로드 · 관리자 화면 · 테스트 코드 · CI.

이 목록은 **협상 대상이 아니다.** 데모 중 요청이 들어와도 4시간 안에는 안 만든다.

> 위 목록은 4시간 타임박스 기준이다. 데모 이후 2차로 채팅(FR-14)과 회원가입(FR-18)은 범위에 다시 넣었다 — §4 「2차 추가」 참조.

---

## 2. 사용자

| 역할 | 정의 | 이 제품에서 하는 일 |
|---|---|---|
| 방문자 | 로그인 전 | 모임을 둘러보고 검색한다. 가입은 못 한다 |
| 회원 | 임시계정 또는 직접 만든 계정으로 로그인 | 가입·탈퇴, 모임 개설, 정모 참석 |
| 모임장 | 모임을 개설한 회원 | 정모를 만든다. 탈퇴 불가(자기 모임) |

**임시계정이 기본이고, 회원가입은 곁길이다.** `demo01`~`demo40` 계정이 시드로 미리 존재하고 비밀번호는 전부 `1234`.
로그인 화면에 `demo01`~`demo08` 8개를 버튼으로 노출해 클릭 한 번에 입력된다.
내 이름으로 들어오고 싶은 사람은 `/signup`에서 계정을 직접 만들 수 있다 (FR-18 · 2026-08-20 추가).
**데모 시나리오는 여전히 임시계정으로 진행한다** — 가입 폼 입력은 무대에서 시간을 잡아먹는다.

---

## 3. 데모 시나리오 (요구사항의 척추)

여기 안 나오는 기능은 만들지 않는다.

| # | 화면 | 동작 | 성공 판정 |
|---|---|---|---|
| 1 | `/login` | `demo03 (박하은)` 버튼 클릭 → 로그인 | 헤더에 "박하은님" |
| 2 | `/` | 카테고리 `운동/스포츠` + 지역 `판교` 선택 | 카드 12 → 3개로 좁혀지고 URL에 필터가 남음 |
| 3 | `/groups/[id]` | 「반코트 배드민턴」 진입 → **가입하기** | 멤버 25 → 26, 멤버 목록에 내 이름 |
| 4 | 같은 화면 | 정모 「8/22 금 20:00」 **참석 신청** | 참석자 목록에 내 이름 |
| 5 | `/groups/new` | 「계모임 클론 스터디」 개설 | 홈 목록 최상단에 등장 |
| 6 | — | **청중에게 URL 공유** → 각자 폰으로 로그인 후 5번 모임에 가입 | 새로고침할 때마다 멤버 수가 오름 |

6번이 하이라이트다. 그래서 §6의 모바일 대응과 §5.3의 시드가 앞쪽 우선순위를 갖는다.

---

## 4. 기능 요구사항

우선순위는 **만드는 순서이자 자르는 순서(역순)**다. P0는 사수, P3부터 차례로 버린다.

### P0 — 절대 사수 (이게 없으면 데모가 성립 안 함)

| ID | 요구사항 | 상세 |
|---|---|---|
| FR-1 | 임시계정 로그인 | id + pw 입력. 로그인 화면에 8개 계정을 버튼으로 노출, 클릭 시 폼 자동 입력. 실패 시 "아이디 또는 비밀번호가 달라요" |
| FR-2 | 로그아웃 | 헤더에서 1클릭. 세션 쿠키 파기 후 `/`로 |
| FR-3 | 모임 목록 | 카드 그리드. 카드 = 커버 · 이름 · 한 줄 소개 · `카테고리 · 지역 · 멤버 N` |
| FR-4 | 카테고리 필터 | 좌측 사이드바 단일 선택(라디오). 14종 + `전체` |
| FR-5 | 지역 필터 | 상단 칩 단일 선택. 12종 + `모든 지역` |
| FR-6 | 모임 상세 | 커버 · 이름 · 소개 · 카테고리/지역 · 멤버 목록(닉네임, 모임장 표시) |
| FR-7 | 가입 | 로그인 상태에서만. 멤버수·멤버목록 즉시 반영. **중복 가입 불가** |
| FR-8 | 탈퇴 | 가입 상태면 버튼이 "탈퇴하기"로 바뀜. **모임장은 탈퇴 불가** |

### P1 — 검색

| ID | 요구사항 | 상세 |
|---|---|---|
| FR-9 | 키워드 검색 | 상단 검색바. 모임 이름·소개에 부분일치. 카테고리·지역 필터와 **동시 적용** |

### P2 — 모임 개설

| ID | 요구사항 | 상세 |
|---|---|---|
| FR-10 | 모임 만들기 | 로그인 필수. 입력: 이름 · 한 줄 소개 · 소개글 · 카테고리 · 지역. 생성자는 자동으로 모임장 멤버십 획득 |

### P3 — 정모 (컷 1순위)

| ID | 요구사항 | 상세 |
|---|---|---|
| FR-11 | 정모 목록 | 모임 상세에 예정 정모를 시간순 표시. `제목 · 일시 · 장소 · 참석 N명` |
| FR-12 | 정모 만들기 | **모임장만**. 입력: 제목 · 일시 · 장소 |
| FR-13 | 참석 신청/취소 | 그 모임 멤버만. 참석자 목록에 즉시 반영. 중복 신청 불가 |

### 2차 추가 (데모 이후 확장, 2026-08-19)

| ID | 요구사항 | 상세 |
|---|---|---|
| FR-14 | 모임 채팅방 | 모임별 채팅. **그 모임 멤버만** 읽고 쓴다. **WebSocket(Supabase Realtime)** 으로 실시간 갱신, 폴백 폴링 20초(D-16·D-17) |
| FR-15 | 마이페이지 | `/me` — 가입한 모임을 **모임장인 것부터** 보여주고 바로 진입. `/me/edit` — 닉네임·한 줄 소개 수정 |
| FR-16 | 회원 탈퇴 | `/me/edit` 하단. 사라지는 것(소유 모임 포함)을 개수로 미리 보여주고, **로그인 아이디를 직접 입력해야** 실행(D-13). 비밀번호 변경은 제공하지 않음(D-12) |
| FR-17 | 모임 관리 | `/groups/[id]/manage` — **모임장 전용**. 멤버 강제 퇴장, 정모 추가·삭제 |
| FR-18 | 회원가입 | `/signup` — 아이디·비밀번호·비밀번호 확인·닉네임. 아이디는 영문 소문자·숫자·밑줄 4~20자, **중복 불가**(UNIQUE). 가입 즉시 세션 발급 → `next`로 이동. 비밀번호는 D-4대로 평문이라 화면에서 **경고 문구**를 보여준다(D-19) |

### 횡단 요구사항

| ID | 요구사항 |
|---|---|
| FR-X1 | 비로그인 상태에서 가입·개설·참석 시도 시 `/login`으로 유도 (에러 페이지 아님) |
| FR-X2 | 모든 목록·상세는 **로그인 없이도 조회 가능** — 청중이 URL만 받고 바로 볼 수 있어야 함 |
| FR-X3 | 상단에 데모 안내 배너: "데모용 서비스입니다. 실제 개인정보를 입력하지 마세요." |

---

## 5. 데이터

### 5.1 테이블 (5개)

```
users        id, login_id UNIQUE, password, nickname, created_at
groups       id, name, summary, description, category, region, owner_id → users, created_at
memberships  id, group_id → groups, user_id → users, role('owner'|'member'), joined_at
             UNIQUE(group_id, user_id)
events       id, group_id → groups, title, starts_at, place, created_by → users, created_at
attendances  id, event_id → events, user_id → users, created_at
             UNIQUE(event_id, user_id)
```

- **카테고리·지역은 테이블이 아니라 TS 상수다.** `groups`에 문자열로 저장한다. 조인을 없애 필터를 단순 비교로 만든다.
- 멤버 수는 별도 컬럼으로 두지 않고 `memberships` 집계로 낸다. 카운터 동기화 버그를 원천 차단.
- UNIQUE 제약 2개가 중복 가입·중복 참석을 DB 레벨에서 막는다. 앱 레벨 체크만 믿지 않는다(청중 동시 접속 시나리오).

### 5.2 상수

- **카테고리(14)** — 운동/스포츠 · 사교/인맥 · 인문학/책/글 · 아웃도어/여행 · 음악/악기 · 업종/직무 · 문화/공연/축제 · 외국/언어 · 게임/오락 · 공예/만들기 · 댄스/무용 · 봉사활동 · 사진/영상 · 자기계발
- **지역(12)** — 용인 · 수원 · 광교 · 판교 · 분당 · 동탄 · 평택 · 부천 · 일산 · 평촌 · 안산 · 남양주

### 5.3 시드 (데모의 무대장치)

- 유저 8명 — `demo01`~`demo08`, pw `1234`, 한국어 닉네임
- 모임 12개 — 카테고리·지역이 골고루 흩어지되, **`운동/스포츠` + `판교` 조합에 정확히 3개**(시나리오 2의 판정 기준)
- 멤버십 — 모임별 5~30명 범위로 흩뿌림. 「반코트 배드민턴」은 정확히 25명(시나리오 3)
- 정모 3개 — 그중 하나는 「반코트 배드민턴」의 `8/22 20:00 판교 체육관`(시나리오 4)

시드는 **재적용 가능한 단일 SQL**로 만든다. 공개 앱이라 데모 전 리셋이 필요하다.

---

## 6. 비기능 요구사항

| 구분 | 요구 |
|---|---|
| 반응형 | **모바일 필수.** 시나리오 6에서 청중이 폰으로 들어온다. 데스크톱은 좌측 사이드바, 모바일은 가로 스크롤 칩으로 전환 |
| 커버 이미지 | 업로드·외부 이미지 없음. **카테고리별 그라데이션 + 이모지**로 생성. 도메인 설정·CSP·로딩 실패를 전부 회피 |
| 보안 | DB 접근은 서버에서만(service_role 키는 클라이언트에 절대 노출 금지). RLS deny-by-default 유지, anon 정책 0개 |
| 비밀번호 | **평문 저장. 의도된 선택이다** — 공개된 데모 계정이고 보호할 자산이 없다. 실서비스 전환 시 최우선 교체 대상 |
| 세션 | httpOnly 서명 쿠키. 만료 7일 |
| 성능 | 목록·상세는 Server Component에서 직접 조회. 별도 API 층을 만들지 않는다 |
| 언어 | UI 문구·오류 메시지는 한국어(해요체). 코드 식별자는 영어 |

---

## 7. 컷 라인 · 성공 기준

**시간표**

| 시각 | 산출 | 게이트 |
|---|---|---|
| 0:20 | 스캐폴드 + Vercel 연결 | 빈 화면이 **라이브 URL**에 뜬다 |
| 0:45 | 스키마 + 시드 | 라이브에서 모임 이름 12개가 보인다 |
| 1:15 | FR-1,2 | 라이브에서 로그인·로그아웃 |
| 2:15 | FR-3,4,5,9 | 라이브에서 필터 조합이 동작 |
| 2:55 | FR-6,7,8 | 라이브에서 가입 → 멤버수 증가 |
| 3:25 | FR-10 | 라이브에서 개설 → 목록 등장 |
| 3:50 | FR-11,12,13 | 라이브에서 참석 신청 ⚠️ **컷 라인** |
| 4:00 | 리허설 | 시나리오 1~6 완주 |

각 구간은 **push → Vercel 배포 → 라이브에서 직접 클릭**까지 해야 끝난 것이다.
로컬에서만 되는 것은 안 된 것으로 친다.

**컷 순서** — 시간이 모자라면 아래에서부터 버린다: ① 정모(FR-11~13) → ② 모임 만들기(FR-10) → ③ 검색(FR-9).

**성공 기준**: 시나리오 1·2·3·5·6이 라이브에서 완주되면 데모는 성공이다. 4번(정모)은 보너스.

---

## 8. 리스크

| # | 리스크 | 대응 |
|---|---|---|
| R1 | Vercel 배포가 마지막에 터진다 | 0:20에 배포를 먼저 끝낸다. 3시간째 처음 만나면 데모가 통째로 날아간다 |
| R2 | Next.js 16이 훈련 데이터와 다르다 | `AGENTS.md`가 경고하는 대로 `node_modules/next/dist/docs/`를 먼저 읽는다. 추측으로 쓰지 않는다 |
| R3 | 공개 비번이라 누구나 데이터를 조작 | 그게 의도다. 대신 시드 리셋 SQL을 준비해 리허설 직전 되돌린다 |
| R4 | 청중 동시 접속으로 중복 가입 레이스 | DB UNIQUE 제약으로 방어. 앱 체크만 믿지 않는다 |
| R5 | 기능 4종을 다 넣으면 4시간 20분 — 이미 초과 | §7 컷 순서를 그대로 따른다. 범위 확대는 없다 |
| R6 | 막힌 지점에서 시간을 태운다 | **15분 룰** — 15분 넘게 안 풀리면 해결 대신 그 기능의 범위를 줄인다 |
